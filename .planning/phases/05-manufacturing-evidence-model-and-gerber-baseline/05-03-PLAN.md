---
phase: 05-manufacturing-evidence-model-and-gerber-baseline
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - Cargo.lock
  - crates/ratemypcb-core/Cargo.toml
  - crates/ratemypcb-core/src/fabrication.rs
  - crates/ratemypcb-core/tests/fabrication_release.rs
  - tests/fixtures/fabrication/gerber/manifest.json
  - tests/fixtures/fabrication/gerber/simple-x2.gbr
  - tests/fixtures/fabrication/gerber/modal-arcs-regions.gbr
  - tests/fixtures/fabrication/gerber/transforms-step-repeat.gbr
  - tests/fixtures/fabrication/gerber/route-file-function.gbr
  - tests/fixtures/fabrication/gerber/invalid-comment-bytes.gbr
  - tests/fixtures/fabrication/gerber/unsupported-semantic.gbr
  - tests/fixtures/fabrication/gerber/mutations.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-PRECLEAN-ATTESTATION.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
requirements: [FAB-03]
autonomous: true
must_haves:
  truths:
    - Full decision verifier is the literal first command before dispatch or edits; any stale/hash/identity/review/remote/Cargo/token failure aborts unchanged.
    - Before STOP/PASS dispatch, a narrow duplicate-safe pre-cleanup attestation hashes every regular Rust production source file under core `src/`, binds exact decision/packet/review hashes, and records ordered timestamp.
    - Exact `STOP_F416` removes dev edge and candidate lock, updates exact STOP summary/VALIDATION/ROADMAP/STATE while FAB-03 remains unchecked in REQUIREMENTS, then `cleaned-stop` verifier proves parser absence, byte-identical production source hashes, and traceability.
    - Exact `PASS_F416` alone promotes identical git URL/full f416 from core dev to core normal dependency; any other token aborts and completed STOP can never unlock production.
    - PASS production preserves original bytes/digest, newline-independent bounded framing, narrow invalid-byte G04 normalization, and accounts for every parser result/error before interpretation.
    - Supported units/formats/modal operations/interpolation/quadrants/apertures/macros/blocks/draws/arcs/regions/polarity/transforms/SR produce deterministic checked fixed-point canonical geometry.
    - Before hostile closure, production verifier reruns and all shell validation stops immediately on first failure; publication capture is a final distinct group only after successful checks.
    - Official corpus requires nonempty external `RATEMYPCB_UCAMCO_CORPUS`, exact ZIP digests, and direct same-buffer member parsing.
    - PASS completion summary explicitly records `status: complete`, `decision: PASS_F416`, and completed FAB-03 so downstream plans can require it in addition to production verifier.
  artifacts:
    - path: .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-PRECLEAN-ATTESTATION.json
      provides: Exact pre-dispatch production-source hash set and authority bindings for cleaned STOP proof.
    - path: crates/ratemypcb-core/src/fabrication.rs
      provides: PASS-only Gerber byte boundary, complete parser accounting, fixed-point interpreter, deterministic geometry, typed failures and resource enforcement.
    - path: .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md
      provides: Exact STOP cleanup/block or completed PASS/FAB-03 evidence consumed downstream.
  key_links:
    - from: 05-02-FORK-DECISION.json
      to: decision/cleaned-stop/production verifier modes
      via: Exact token and packet/review/time/identity bindings at each state transition.
    - from: pre-cleanup attestation source hashes
      to: cleaned-stop verifier
      via: Sorted path/SHA-256 comparison proves STOP touched no production source byte.
    - from: PASS git dependency
      to: exact remote ref
      via: Full f416 Cargo source plus exact-ref fetched tree e7fb.
    - from: framed blocks and parser results/errors
      to: canonical geometry
      via: Complete accounting precedes checked fixed-point interpretation.
---

<objective>
Dispatch exact STOP_F416/PASS_F416 with auditable pre-cleanup source hashes; prove STOP cleanup without production changes or, only on strict PASS, promote identical source and close FAB-03 production Gerber semantics/hostile/corpus evidence.
</objective>

<ownership>
This plan owns first-command decision verification, pre-cleanup source attestation, exact STOP cleanup/traceability and cleaned-stop proof, exact PASS promotion, production Gerber boundary/accounting/interpreter, Gerber hostile/resource/sanitized/official corpus tests, publication-block diagnostic, and terminal FAB-03 result. It does not own package/XNC/reconciliation or fork mutation.
</ownership>

<non_goals>

- No production after STOP or invalid decision; no candidate change/repair/fallback.
- No FAB-04/05/06/08, XNC, Job/package/reconciliation/product integration; Plans 05-04..06 own old package tasks.
- No publication, release, ODB++, IPC-2581, DFM policy, or official corpus redistribution.
</non_goals>

<context>
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-CONTEXT.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-CANDIDATE.json
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-REVIEW.json
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-DECISION.json
@tests/verify_gerber_fork_candidate.py
@Cargo.lock
@crates/ratemypcb-core/Cargo.toml
@crates/ratemypcb-core/src/fabrication.rs
@crates/ratemypcb-core/tests/fabrication_release.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>05-03-01: Attest and dispatch exact STOP_F416 or PASS_F416</name>
  <files>Cargo.lock, crates/ratemypcb-core/Cargo.toml, crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/tests/fabrication_release.rs, tests/verify_phase5_summary.py, tests/fixtures/fabrication/gerber/manifest.json, tests/fixtures/fabrication/gerber/simple-x2.gbr, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-PRECLEAN-ATTESTATION.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-VALIDATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - Authority verifier and immutable Plan 05-02 candidate/review/decision JSON.
    - Current manifests/lock and all regular core production Rust source paths; do not edit before verification/attestation.
    - Existing planning rows for exact STOP/PASS result updates.
  </read_first>
  <behavior>
    - Decision mode runs first; invalid authority causes no file write including no attestation.
    - Attestation contains sorted relative production source paths and SHA-256, no absolute paths, exact authority hashes and time.
    - STOP cleanup leaves source hash map identical and parser absent, FAB-03 pending, downstream plans blocked.
    - PASS promotion is identical URL/rev/graph and production verifier passes before source edit.
    - PASS tracer produces original-digest-linked newline-independent 9/9 fixed-point simple geometry.
  </behavior>
  <action>
    Literal first command is `python3 tests/verify_gerber_fork_candidate.py --mode decision`; abort unchanged on nonzero. Then create `05-03-PRECLEAN-ATTESTATION.json` using stdlib duplicate-free JSON: exact decision/packet/review SHA-256, UTC timestamp, and sorted relative path/SHA-256 for every regular non-symlink `*.rs` beneath `crates/ratemypcb-core/src`. Refuse duplicate/unsafe paths. Parse exact token only after attestation; accept only STOP_F416 or PASS_F416.

    Create one stdlib `tests/verify_phase5_summary.py` before dispatch. It parses only the first YAML frontmatter block, rejects duplicate keys, accepts only exact `status`, optional exact `decision`, and an exact ordered `requirements_completed` bracket array, and compares each requirement against an exact checked/unchecked row in `.planning/REQUIREMENTS.md`; prose mentions never count.

    For STOP_F416: remove dev git edge, regenerate lock without candidate graph, and do not edit any production source. Write duplicate-free terminal summary frontmatter with exact `status: stopped`, `decision: STOP_F416`, `requirements_completed: []`, plus cleanup commands/hashes in the body. Update only exact result rows: VALIDATION marks 05-03 STOP cleanup green but production blocked; ROADMAP keeps 05-03 incomplete/blocked; STATE stops at verified fork STOP; REQUIREMENTS keeps FAB-03 unchecked. Run `python3 tests/verify_gerber_fork_candidate.py --mode cleaned-stop` and `python3 tests/verify_phase5_summary.py --summary .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md --status stopped --decision STOP_F416 --requirements ''`; together they revalidate all authority artifacts/decision, parser absence from all manifest/lock/source locations, attested source bytes, exact summary state, and exact unchecked requirement state. End Plan 05-03; do not enter PASS code or mark completion.

    For PASS_F416: move exact literal fork URL/full f416 from core dev to core normal dependencies, remove dev duplicate, regenerate lock narrowly, and run production verifier before source edits. Implement bounded original-byte `GerberByteBoundary`, newline-independent `*`/`%` framing, parser-copy-only ordinary nonsemantic G04 normalization with exact spans, complete block/fragment-to-result/error reconciliation, banned filtering helpers, typed unsupported omissions, and tracer explicit MO/FS checked picometres, aperture, D02/D01, M02, stable feature identity/extents/digest. Preserve exact Route parser evidence for Plan 05-04 and never suppress siblings.
  </action>
  <verify><automated>set -euo pipefail; token="$(python3 -c 'import json; p=".planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-DECISION.json"; unique=lambda pairs: ({k:v for k,v in pairs} if len({k for k,_ in pairs})==len(pairs) else (_ for _ in ()).throw(ValueError("duplicate JSON key"))); print(json.load(open(p),object_pairs_hook=unique)["decision"])')"; if test "$token" = STOP_F416; then python3 tests/verify_gerber_fork_candidate.py --mode cleaned-stop; python3 tests/verify_phase5_summary.py --summary .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md --status stopped --decision STOP_F416 --requirements ''; elif test "$token" = PASS_F416; then python3 tests/verify_gerber_fork_candidate.py --mode production; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_semantics_tracer_; else exit 1; fi</automated></verify>
  <acceptance_criteria>
    - Full decision verifier is first and attestation precedes dispatch edits.
    - STOP cleaned-stop mode passes authority, parser absence, byte-identical production source and exact traceability; 05-03 remains incomplete and downstream blocked.
    - PASS alone promotes identical full-rev source and production verifier passes before source edit.
    - PASS tracer proves complete 9/9 accounting, original digest, checked fixed-point geometry, deterministic identity/digest and no approval shortcut.
  </acceptance_criteria>
  <done>STOP is cryptographically clean and terminal, or exact PASS has one verified bounded production Gerber path.</done>
</task>

<task type="auto" tdd="true">
  <name>05-03-02: Complete PASS-only Gerber semantics, hostile/resource/corpus and FAB-03 result</name>
  <precondition>Exact decision is PASS_F416 and Task 05-03-01 production verifier/tracer passed; STOP_F416 or stopped summary is a hard block.</precondition>
  <files>crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/tests/fabrication_release.rs, tests/fixtures/fabrication/gerber/manifest.json, tests/fixtures/fabrication/gerber/modal-arcs-regions.gbr, tests/fixtures/fabrication/gerber/transforms-step-repeat.gbr, tests/fixtures/fabrication/gerber/route-file-function.gbr, tests/fixtures/fabrication/gerber/invalid-comment-bytes.gbr, tests/fixtures/fabrication/gerber/unsupported-semantic.gbr, tests/fixtures/fabrication/gerber/mutations.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-VALIDATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - PASS tracer, production verifier, full resource contract, and retained Gerber spike/resource fixtures.
  </read_first>
  <behavior>
    - Production verifier is first; STOP/stale state cannot run this task.
    - Supported MO/FS/modal D01/D02/D03/apertures/macros/blocks/arcs/regions/polarity/transforms/SR are exact checked fixed-point.
    - Every unsupported/malformed/modal/overflow/truncation/terminator/resource condition is typed incomplete/failed with no partial success.
    - Direct verified-buffer official corpus is mandatory and deterministic.
    - Publish diagnostic is last and cannot run after/mask any failed check.
  </behavior>
  <action>
    First run production verifier and nonempty corpus guard; abort before edits. Expand old Plan 05-02 production scope fully: explicit MO/FS, checked decimals to i64 picometres, modal axes/operations, standard apertures and supported macros/variables/operations/blocks, exact linear/CW/CCW arcs/quadrants, region contours/cut-ins, LP/LM/LR/LS, bounded SR, terminator rules, deterministic IDs/extents/capabilities/omissions/conflicts/digest. No floats or silent records; X2 records remain for Plan 05-04 without FAB-04 claim.

    Add positive/hard-negative/truncation/mutation tests for each semantic family and all declared byte/aggregate/command/record/token/text/metadata/numeric/nesting/aperture/macro/block/SR/feature/vertex/allocation/time exact/over limits during lexing, accounting, interpretation and expansion. Cover invalid UTF-8 zones, controls, compact/CR/LF/CRLF, multiline macros, delimiter-free/unclosed extended blocks, every command truncation, undefined state and expansion bombs. No panic/hang/truncated success.

    Require external corpus, verify two recorded ZIP digests, and parse members directly from those same in-memory buffers. Record fresh records/errors/normalizations/Route/geometry/bounds/digests and explain differences; unavailable 2026 ZIP remains gap. Validate sanitized manifest and no official bytes.

    Use `set -euo pipefail` for focused/full checks. Only after all pass, run separate publish capture; unexpected success or failure not naming `gerber_parser` plus git/repository source fails. Then write duplicate-free summary frontmatter with exact fields `status: complete`, `decision: PASS_F416`, `requirements_completed: [FAB-03]`; update actual FAB-03 result in VALIDATION/ROADMAP/STATE/REQUIREMENTS without marking Phase 5 complete. Run the shared summary verifier against exact complete/PASS/FAB-03 and the checked REQUIREMENTS row; prose mentions do not satisfy it.
  </action>
  <verify><automated>set -euo pipefail; python3 tests/verify_gerber_fork_candidate.py --mode production; : "${RATEMYPCB_UCAMCO_CORPUS:?set RATEMYPCB_UCAMCO_CORPUS}"; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_semantics_; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_hostile_; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_corpus_; RATEMYPCB_UCAMCO_CORPUS="$RATEMYPCB_UCAMCO_CORPUS" cargo test -p ratemypcb-core --test fabrication_release --locked gerber_official_production_regression -- --nocapture; cargo fmt --all -- --check; cargo clippy --all-targets -- -D warnings; cargo test --all --locked; node --test tests/*.test.mjs; git diff --check; publish_log="$(mktemp /tmp/ratemypcb-publish.XXXXXXXX.log)"; if cargo publish -p ratemypcb-core --dry-run --allow-dirty --locked &gt;"$publish_log" 2&gt;&amp;1; then cat "$publish_log"; exit 1; fi; grep -F gerber_parser "$publish_log"; grep -Ei 'git|repository' "$publish_log"; python3 tests/verify_phase5_summary.py --summary .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-03-SUMMARY.md --status complete --decision PASS_F416 --requirements FAB-03</automated></verify>
  <acceptance_criteria>
    - First command rejects STOP/stale/nonproduction state.
    - All supported RS-274X families produce deterministic fixed-point semantics with complete accounting.
    - All malformed/resource boundaries fail closed without panic/hang/silent/truncated success.
    - Sanitized and direct verified-buffer official corpus evidence passes with no restricted bytes tracked.
    - Shell exits on first failed validation; publish capture runs last and proves expected git dependency block.
    - Summary/traceability uniquely complete FAB-03 under PASS_F416 and provide downstream unlock evidence.
  </acceptance_criteria>
  <done>Exact PASS_F416 closes FAB-03; STOP or any verifier/evidence failure cannot produce a completed 05-03.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

- Pre-cleanup production source attestation and cleaned-stop proof, or identical-source PASS promotion.
- PASS-only fixed-point Gerber production adapter and complete hostile/resource/sanitized/direct-corpus evidence.
- `tests/verify_phase5_summary.py` duplicate-free structural summary verifier used by downstream planning checks.
- Exact STOP blocked or PASS complete summary/traceability consumed by downstream plans.
</artifacts_this_phase_produces>

<verification>
Decision verifier is first, attestation precedes dispatch, cleaned-stop validates STOP after cleanup/traceability, and production verifier gates PASS edits and hostile closure. All shell checks use immediate failure; publication capture is last.
</verification>

<acceptance_exit_criteria>

- STOP preserves production source byte-for-byte, removes parser, records exact blocked traceability, and cannot unlock downstream.
- PASS retains exact f416 source and closes complete bounded deterministic FAB-03 only.
- Downstream requires both production verifier and explicit completed PASS/FAB-03 summary.
</acceptance_exit_criteria>

<honest_deferrals>
Old package task 01 is Plan 05-04, task 02 Plan 05-05, task 03 Plan 05-06. ODB++/IPC-2581, DFM, release and known legal/corpus deferrals remain unchanged.
</honest_deferrals>

<output>Create exact STOP or PASS `05-03-SUMMARY.md`; completed STOP is not plan success and cannot satisfy downstream preconditions.</output>
