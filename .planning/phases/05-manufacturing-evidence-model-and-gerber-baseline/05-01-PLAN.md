---
phase: 05-manufacturing-evidence-model-and-gerber-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ratemypcb-core/src/fabrication.rs
  - crates/ratemypcb-core/src/lib.rs
  - crates/ratemypcb-core/src/stackup.rs
  - crates/ratemypcb-core/tests/fabrication_release.rs
  - schemas/report-2.0.json
requirements: [FAB-01, FAB-02, FAB-07]
autonomous: true
must_haves:
  truths:
    - One provenance-aware canonical model represents manufacturing identity, layers, fixed-point geometry/transforms, tools, profile, connectivity, assembly, construction, constraints, omissions, conflicts, and capability completeness without format-specific approval policy.
    - Every analyzer declares exact capability prerequisites; missing, partial, stale, failed, unsupported, or omitted capability produces not-checked/non-pass evidence and cannot improve approval.
    - Canonical IDs and digests are deterministic, duplicate-checked, independent of prose/package ordering/local paths, and sensitive to semantic/source changes.
    - Filename/token screening, filename-derived stackup, and browser Gerber parsing are visibly partial/non-approval evidence and cannot pass required semantic fabrication coverage.
    - Exact file/aggregate/token/record/depth/text/numeric/geometry/allocation/time budgets are serialized or code-visible and a limit breach is never truncated success.
  artifacts:
    - path: crates/ratemypcb-core/src/fabrication.rs
      provides: Canonical manufacturing DTOs, fixed-point/unit/transform contracts, capabilities/omissions/conflicts, budgets, deterministic identities/digests, adapter results, and capability-gated analyzer dispatch.
    - path: crates/ratemypcb-core/tests/fabrication_release.rs
      provides: Model, fixed-point, identity, capability truth-table, hostile-limit, and legacy-screening regressions.
    - path: crates/ratemypcb-core/src/lib.rs
      provides: Byte-preserving manufacturing inventory and additive report/evidence/gate integration.
    - path: schemas/report-2.0.json
      provides: Generated additive manufacturing evidence contract.
  key_links:
    - from: crates/ratemypcb-core/src/lib.rs::load_path/load_zip
      to: crates/ratemypcb-core/src/fabrication.rs::ManufacturingInput
      via: Original bytes, normalized virtual path, exact digest, load outcome, and source bounds enter one adapter boundary.
    - from: crates/ratemypcb-core/src/fabrication.rs::AnalyzerRequirements
      to: crates/ratemypcb-core/src/fabrication.rs::CapabilityLedger
      via: Analyzer dispatch emits not_checked unless every named prerequisite is complete.
    - from: crates/ratemypcb-core/src/lib.rs::review/finalize_evidence
      to: Report.fabrication and required fabrication coverage
      via: Canonical model and capability digests supply Phase 1 provenance while approval remains core-owned.
    - from: crates/ratemypcb-core/src/stackup.rs::Stackup::from_gerbers
      to: Report fabrication capability ledger
      via: Filename inference is retained only as weak partial inventory and is never canonical construction evidence.
---

<objective>Establish the canonical provenance-aware manufacturing model and fail-closed capability contract, then retire filename/token screening as approval evidence before any production parser dependency is adopted.</objective>

<ownership>
This plan owns canonical manufacturing types, fixed-point/unit/transform semantics, resource-budget records, deterministic IDs/digests, adapter/analyzer separation, byte-preserving manufacturing inventory, report/schema scaffolding, and honest downgrade of legacy screening. It does not add `gerber_parser`, parse production Gerber geometry, parse XNC, interpret Gerber Job, reconcile native KiCad with a release package, or add Phase 7 DFM rules.
</ownership>

<non_goals>

- No Gerber parser dependency or production Gerber/XNC adapter.
- No ODB++ or IPC-2581 model shortcut/product adapter.
- No clearance, annular-ring, mask, paste, thermal, return-path, creepage, or fabricator tolerance policy.
- No viewer-side evidence generation, release publication, provider change, or KiCad support-matrix change.

</non_goals>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-CONTEXT.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-RESEARCH.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-VALIDATION.md
@.planning/phases/01-decision-first-evidence-contract/01-CONTEXT.md
@.planning/phases/01-decision-first-evidence-contract/01-VERIFICATION.md
@.planning/phases/04-kicad-schematic-release-evidence/04-VERIFICATION.md
@crates/ratemypcb-core/src/lib.rs
@crates/ratemypcb-core/src/stackup.rs
@crates/ratemypcb-cli/assets/board-view.js
@schemas/report-2.0.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>05-01-01: Define fixed-point canonical manufacturing evidence and deterministic identity</name>
  <files>crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json</files>
  <action>Start `fabrication_release.rs` with failing `model_` tests, then add one plain `fabrication.rs` module. Define additive report DTOs for `FabricationReview`, product/package/document identity, layer role/side/context/polarity/order and authority, canonical points/lines/arcs/contours/regions/flashes, ordered transforms, apertures/macros/blocks/repetitions, drills/routes/slots/tools/plating/spans, profile/cutouts/extents, optional net/component/pin semantics, assembly/construction/constraints, provenance, warnings, omissions, conflicts, and a capability ledger. Use checked signed `i64` picometres, source unit/format/resolution, decimal-string parsing with `i128` intermediates, maximum nine decimal places and ±10 m extent; no production `f32`/`f64` geometry. Preserve transforms as ordered mirror, microdegree rotation, and rational scale operations; exact right-angle materialization must be lossless, and any non-right-angle materialization must use one deterministic checked fixed-point routine with an explicit quantization/error record or leave the transformed capability partial. Preserve arcs exactly rather than chord-flattening them. Encode the exact Phase 5 resource table in a `ManufacturingLimits` constant/serialized diagnostic contract and check estimated canonical allocation. Define version-tagged document/layer/tool/feature/package IDs and canonical model digest over sorted semantic records; exclude prose, diagnostics, wall time, absolute paths, and inventory order. Validate lowercase SHA-256, referential integrity, duplicates, bounds, unsupported enum states, omission-to-capability linkage, and conflict provenance. Add `Report.fabrication` with a safe not-provided default, runtime validation, and generated schema equality while preserving report schema version 2.0.</action>
  <verify><automated>cargo test -p ratemypcb-core --test fabrication_release --locked model_ &amp;&amp; cargo test -p ratemypcb-core --locked decision_contract_generated_schemas_match_checked_in_json</automated></verify>
  <acceptance_criteria>
    - Exact mm/inch decimal conversions, source resolution, negative coordinates, right-angle transforms, and boundary values pass without floats.
    - Finer-than-contract decimals, ±10 m overflow, checked arithmetic overflow, invalid rational scale, non-deterministic/hidden quantization, excessive depth/records/text/model size, duplicate IDs, bad digests, and dangling references fail with typed errors.
    - Reordering documents/features/warnings or changing warning prose leaves canonical IDs/digest unchanged; changing artifact bytes, semantic type, source structural location, geometry, role, or capability changes the appropriate identity/digest.
    - The model can represent every FAB-01 domain and explicit unknown/omission/conflict states without using a format badge as capability.
    - Parsed generated schema equals `schemas/report-2.0.json`, and report validation independently recomputes the fabrication model digest.
  </acceptance_criteria>
  <done>The report has one validated deterministic manufacturing model contract with checked fixed-point geometry, explicit provenance/completeness, and executable resource limits.</done>
</task>

<task type="auto" tdd="true">
  <name>05-01-02: Separate adapters from analyzers and retire legacy screening authority</name>
  <files>crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/src/stackup.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json</files>
  <action>Replace `Loaded.manufacturing: Vec&lt;(String,String,String)&gt;` with byte-preserving bounded manufacturing inputs/outcomes that retain normalized virtual path, original bytes SHA-256, kind candidate, size, and explicit omitted/failed reason. Enforce 256 files, 4 MiB/file, 20 MiB aggregate, and the declared Phase 5 path/archive bounds without silently skipping over-limit files. Add a minimal adapter-result contract containing facts plus capabilities/omissions/conflicts only; prohibit approval/gate output in adapter types. Add analyzer declarations with stable check families and exact prerequisite capability IDs, plus a dispatcher truth table: only all-`complete` prerequisites may produce a semantic result; partial/not-provided/unsupported/failed/stale/omitted yields `not_checked` and cannot reduce risk or improve approval. Route `package-gerbers`, `gerber-syntax`, and `drill-data` through this ledger while no production adapter exists. Delete `gerber_syntax_valid` as a pass source; preserve any filename/token observations only as `legacy-filename-screening`/`legacy-token-screening` partial capability and `evidence_only` limitation records. Make package filename role and mask checks partial even when names look complete. Remove the report fallback that treats `Stackup::from_gerbers` as construction evidence; retain the helper only if existing callers need it, renamed/worded as filename inference and unable to create complete construction capability. Add source assertions/tests that `board-view.js` remains presentation-only and never feeds `review`, required evidence, or approval. Preserve Gerber/X2+Excellon as the named baseline and the existing required check IDs; before Plans 02-03 complete, those checks must close approval as attention/not-run/not-provided rather than false pass.</action>
  <verify><automated>cargo test -p ratemypcb-core --test fabrication_release --locked capability_ &amp;&amp; cargo test -p ratemypcb-core --locked legacy_fabrication_ &amp;&amp; cargo test --all --locked &amp;&amp; node --test tests/board-view.test.mjs tests/report-contract.test.mjs</automated></verify>
  <acceptance_criteria>
    - Adapter DTOs contain no approval, score, severity, gate-impact, or pass policy field.
    - Every capability-state/prerequisite mutation has a named truth-table test; no incomplete state produces pass or improves `approvalEligible`/observed risk.
    - A token-perfect and filename-perfect fake package remains partial/non-approval; malformed or over-limit bytes retain an explicit failure/omission rather than disappearing.
    - `package-gerbers`, `gerber-syntax`, and `drill-data` stay stable public check families but cannot pass until their production semantic adapters and package analyzer complete.
    - Gerber filename-derived layer count is absent from authoritative report stackup/construction evidence.
    - Browser Gerber parsing remains usable for visualization tests but has no link into core evidence or approval.
  </acceptance_criteria>
  <done>The parser/analyzer boundary is enforceable and every legacy fabrication shortcut is honestly downgraded before deeper parsing begins.</done>
</task>

</tasks>

<verification>
Run `cargo fmt --all -- --check &amp;&amp; cargo clippy --all-targets -- -D warnings &amp;&amp; cargo test --all --locked &amp;&amp; node --test tests/*.test.mjs &amp;&amp; git diff --check`. Generate the report schema with the CLI and compare it byte-for-byte with `schemas/report-2.0.json`. Confirm `Cargo.toml`, `Cargo.lock`, and production dependencies contain neither `gerber_parser` nor `gerberx2` in this plan.
</verification>

<acceptance_exit_criteria>

- FAB-01, FAB-02, and FAB-07 are covered by focused green tests and additive report/schema validation.
- Every manufacturing artifact is retained as bytes or an explicit bounded omission/failure.
- Canonical numeric, transform, capability, provenance, identity, digest, and budget contracts are fixed before dependency adoption.
- Required fabrication checks cannot pass from filenames, tokens, `Stackup::from_gerbers`, or browser parsing.
- Plan 05-02 can implement one adapter without changing model/gate semantics.

</acceptance_exit_criteria>

<honest_deferrals>
Production Gerber/X2 parsing and dependency adoption are 05-02. XNC, Gerber Job, package completeness, native/package reconciliation, user surfaces, official XNC closure, and final independent review are 05-03. ODB++/IPC-2581, calibrated DFM policy, release hardening, publication, and preserved Phase 2/provider/KiCad deferrals remain outside this plan.
</honest_deferrals>

<output>After completion, create `.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-01-SUMMARY.md`.</output>
