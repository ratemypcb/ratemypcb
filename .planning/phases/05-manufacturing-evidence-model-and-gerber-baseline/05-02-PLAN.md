---
phase: 05-manufacturing-evidence-model-and-gerber-baseline
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - ../gerber-parser/src/parser.rs
  - ../gerber-parser/tests/component_tests.rs
  - Cargo.lock
  - crates/ratemypcb-core/Cargo.toml
  - crates/ratemypcb-core/tests/fabrication_release.rs
  - tests/verify_gerber_fork_candidate.py
  - tests/test_verify_gerber_fork_candidate.py
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-PUSH-DECISION.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-CANDIDATE.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-REVIEW.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-DECISION.json
  - .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-SUMMARY.md
requirements: [FAB-03]
autonomous: false
must_haves:
  truths:
    - Historical human STOP for crates.io `gerber_parser` 0.5.0 remains immutable in `05-02-DEPENDENCY-CHECKPOINT.md`; no old pin, graph, packet, review, or corpus result becomes fork approval.
    - Existing user authorization covers only `gh repo fork MakerPnP/gerber-parser --org ratemypcb --clone=false` and clone into `../gerber-parser` after both nonexistence and non-symlink checks; no redundant fork gate is added.
    - Upstream tokenizer foundation is exact `f4160c7c6ca1b4cdd9c5273a3916b4fd087b5e34`, parent `8a07cc6064894cbf63978012969af5c1f656a30b`, tree `e7fb208975130e9c1f41019ba75cb7c960dbf02f`; a665 and 5ac remain excluded. Local mutation evidence proved f416 still drops an outer `parse_line` error, so the candidate adds only a non-breaking error-funnel patch with SHA-256 `f9c64bbabd9731ccb68ce8708c64048fe8ae4fe7aff20931062504448d3c1787` over `src/parser.rs` and `tests/component_tests.rs`.
    - Temporary path-only core dev dependency updates stale registry assertions and compact expectation to 9/9, and proves every complete/malformed fragment is represented by a parser success/error before any remote candidate mutation.
    - Remote checkpoint uses exactly `APPROVE_PUSH` and `REFUSE_PUSH` as option IDs, names, response tokens, and dispatch values; REFUSE cleanup is executable rather than prose-only.
    - `REFUSE_PUSH` removes the temporary path edge/candidate lock graph, verifies no parser production import, records STOP, and ends; `APPROVE_PUSH` alone permits one signed local commit with exact parent/message/diff/file hashes, exact ref protection/ruleset, and one non-force push.
    - Approved candidate is consumed dev-only by exact fork URL and the resulting full commit SHA, with deterministic source archive, immutable JSON packet/review, complete supply-chain/corpus/resource evidence, and independent review.
    - Stdlib authority verifier rejects duplicate JSON keys, stale timestamps, packet hash drift, nonempty P0/P1/P2, wrong remote ref/tree, and wrong Cargo placement; table-driven negative tests prove each exits nonzero.
    - Before adoption checkpoint, verifier validates current timestamps/hashes, URL/base/head/tree/archive, ACCEPT plus empty P0/P1/P2, exact remote ref/full SHA/fetched tree, and Cargo full SHA/dev placement.
    - Final checkpoint uses exact `PASS_F416` and `STOP_F416`; decision JSON binds the human token to current packet/review hashes and does not promote production.
  artifacts:
    - path: tests/verify_gerber_fork_candidate.py
      provides: Python-stdlib authority verifier modes candidate, decision, production, and cleaned-stop.
    - path: tests/test_verify_gerber_fork_candidate.py
      provides: Table-driven fail-closed negative matrix for malformed/stale/drifted authority evidence.
    - path: .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-CANDIDATE.json
      provides: Immutable candidate identity, archive, governance, supply-chain, regression, resource, corpus, and recommendation evidence.
    - path: .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-REVIEW.json
      provides: Independent ACCEPT/REJECT review bound to candidate hash and exact identities.
    - path: .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-DECISION.json
      provides: Exact human PASS_F416/STOP_F416 token bound to candidate/review hashes.
    - path: crates/ratemypcb-core/tests/fabrication_release.rs
      provides: Updated fork source/compact/result-error/corpus/resource spike regressions.
  key_links:
    - from: exact f416 sibling tree
      to: temporary path dev dependency then immutable git dev dependency
      via: Local accounting succeeds before push and final Cargo source resolves same full SHA/tree.
    - from: checkpoint token APPROVE_PUSH or REFUSE_PUSH
      to: post-checkpoint task
      via: Exact equality dispatch executes either cleanup-and-stop or push-and-packet, never fallthrough.
    - from: candidate JSON
      to: review and decision JSON
      via: Duplicate-key-safe JSON parsing, SHA-256 bindings, ordered RFC3339 timestamps, and repeated identities.
    - from: exact remote ref
      to: authority verifier
      via: Exact-ref ls-remote plus exact-ref fetch verifies full SHA and tree rather than querying a raw SHA.
---

<objective>
Create and locally validate the authorized RateMyPCB fork from exact f416 plus one minimal non-breaking outer-error funnel, separately gate its signed commit/ref mutation, stage it dev-only by immutable revision, and produce strictly verified independent and human adoption evidence without production code.
</objective>

<ownership>
This plan owns authorized fork creation/local clone, exact f416 provenance, the demonstrated RED outer-error mutation, one minimal non-breaking local error-funnel patch, stale harness corrections, exact commit/push checkpoint and both dispatch outcomes, one approved signed commit/protected ref/non-force push, dev-only full-rev staging, deterministic archive, JSON authority artifacts/verifier/negative matrix, fresh review, and exact adoption token. It uses the dirty current checkout narrowly and creates no worktree, stash, reset, clean, RateMyPCB commit, PR, merge, tag, release, or publication.
</ownership>

<non_goals>

- No wholesale a665 error-context API or 5ac grammar expansion; only the demonstrated six-line non-breaking outer-error funnel and its focused test are in candidate scope.
- No production dependency, byte boundary, adapter, interpreter, or FAB-03 closure; Plan 05-03 owns exact dispatch and production.
- No a665, 5ac, moving ref dependency, registry/version fallback, patch, vendoring, or `gerberx2` production edge.
- No XNC, Gerber Job/package/reconciliation, FAB-04/05/06/08, ODB++, IPC-2581, DFM policy, release, or product approval.
</non_goals>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-CONTEXT.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-VALIDATION.md
@.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-DEPENDENCY-CHECKPOINT.md
@Cargo.lock
@crates/ratemypcb-core/Cargo.toml
@crates/ratemypcb-core/tests/fabrication_release.rs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>05-02-01: Create authorized fork safely and prove exact f416 locally</name>
  <files>../gerber-parser/src/parser.rs, ../gerber-parser/tests/component_tests.rs, Cargo.lock, crates/ratemypcb-core/Cargo.toml, crates/ratemypcb-core/tests/fabrication_release.rs, tests/verify_gerber_fork_candidate.py, tests/test_verify_gerber_fork_candidate.py</files>
  <read_first>
    - Historical dependency checkpoint and fixed Phase 5 error/resource/legal contracts.
    - Current core manifest/lock and `fabrication_release.rs` stale registry/compact assumptions; save narrow before copies.
    - Exact upstream base/f416/a665/5ac ancestry and diffs.
  </read_first>
  <behavior>
    - Both `test ! -e ../gerber-parser` and `test ! -L ../gerber-parser` run before GitHub operation; existing entry or dangling symlink aborts.
    - f416 exact parent/tree/licenses/upstream tests pass and a665/5ac are not included; the one pre-existing Rust 1.96 `blocks_in_conditions` test lint is proven base-owned and narrowly allowed without source change.
    - RED evidence records a standalone `Q*` framed block disappearing on f416; the local patch changes only `src/parser.rs` and `tests/component_tests.rs`, preserves the public error API, and makes that block one contextual `UnknownCommand` error.
    - Temporary `path = "../../../gerber-parser"` exists only under core dev dependencies.
    - Compact input yields 9 records/9 successes/0 errors; inserting `Q*` before each command yields 10 records/9 successes/1 error, and malformed ordinary/extended cases remain fail-closed.
  </behavior>
  <action>
    Run the two absent-path checks before any remote call. Then execute the already-authorized fork command exactly and clone the verified public `ratemypcb/gerber-parser` fork into the proven-absent sibling. Add read-only upstream, fetch PR #25, detach at f416, and create no branch/push. Prove exact parent 8a07, tree e7fb, changed-file allowlist, byte-identical licenses, source/API/manifest/dependency non-change, signature status, and exclusion of a665/5ac; run upstream fmt/Clippy/all-feature tests.

    Add only the temporary path dev dependency and narrow lock graph. Update stale `fabrication_release.rs` registry/lock assertions to understand temporary path then final git, and replace compact silent-loss expectation with exact 9/9 success. First retain RED evidence that standalone `Q*` inserted before each logical command is silently dropped by unmodified f416. Then apply only the reviewed non-breaking funnel in `parse`: convert outer `parse_line` `Err` into `vec![Err(error)]` and iterate the resulting vector without `flatten`. Add upstream `outer_parse_line_errors_are_retained` plus RateMyPCB command-position mutations proving 10 records/9 successes/1 error. Require local patch diff SHA-256 `f9c64bbabd9731ccb68ce8708c64048fe8ae4fe7aff20931062504448d3c1787`, source SHA-256 `170003700d3fe343667e00b4c7ad225ccb0b71f6c5a35fb170cc5a128080f366`, test SHA-256 `29bb344de8ff7e6c741b861479d11a3891802aa5f320a6cd4d4d801941fa6980`, and no other changed path.

    Create `tests/verify_gerber_fork_candidate.py` with stdlib only. Parse JSON via `object_pairs_hook` that rejects duplicate keys. Modes `candidate`, `decision`, `production`, and `cleaned-stop` validate exact constants, ordered nonfuture RFC3339 timestamps, packet/review/decision hashes, archive reproduction, ACCEPT and empty P0/P1/P2, exact remote ref ls-remote/fetch SHA/tree, Cargo metadata/lock full SHA, and manifest placement. `cleaned-stop` additionally checks pre-cleanup attestation, parser absence, byte-identical production source hashes, and exact STOP summary/VALIDATION/STATE/REQUIREMENTS/ROADMAP states. CLI exposes no bypass/mock option.

    Add small table-driven stdlib test `tests/test_verify_gerber_fork_candidate.py`. Build one valid in-memory/temp baseline using injectable internal probe functions, then mutate one case at a time: duplicate JSON key, stale timestamp, packet hash drift, nonempty P0, P1, or P2, wrong remote ref, wrong fetched tree, dev-vs-normal wrong Cargo placement. Each invokes validation/CLI-equivalent path and asserts nonzero/SystemExit with named diagnostic; baseline succeeds. Probe injection is import-only for tests and unavailable from production CLI.
  </action>
  <verify><automated>test "$(git -C ../gerber-parser rev-parse HEAD)" = f4160c7c6ca1b4cdd9c5273a3916b4fd087b5e34 &amp;&amp; test "$(git -C ../gerber-parser rev-parse HEAD^)" = 8a07cc6064894cbf63978012969af5c1f656a30b &amp;&amp; test "$(git -C ../gerber-parser rev-parse HEAD^{tree})" = e7fb208975130e9c1f41019ba75cb7c960dbf02f &amp;&amp; test "$(git -C ../gerber-parser diff --name-only | sort | tr '\n' ' ')" = "src/parser.rs tests/component_tests.rs " &amp;&amp; test "$(git -C ../gerber-parser diff --binary HEAD -- src/parser.rs tests/component_tests.rs | shasum -a 256 | awk '{print $1}')" = f9c64bbabd9731ccb68ce8708c64048fe8ae4fe7aff20931062504448d3c1787 &amp;&amp; (cd ../gerber-parser &amp;&amp; cargo fmt --all -- --check &amp;&amp; cargo clippy --all-targets --all-features -- -D warnings -A clippy::blocks_in_conditions &amp;&amp; cargo test --all-features) &amp;&amp; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_adoption_spike_ &amp;&amp; python3 tests/test_verify_gerber_fork_candidate.py</automated></verify>
  <acceptance_criteria>
    - Authorized fork/clone follows both absent-path checks and creates no pre-gate candidate ref.
    - Exact f416 provenance is preserved; its demonstrated outer-error RED case is repaired only by the bound two-file local diff, whose public API and dependencies are unchanged.
    - Stale registry assertions are removed; temporary edge is path-only/dev-only with no production import.
    - Verifier implements all four strict modes, duplicate-key rejection, and no CLI bypass.
    - Every required table mutation exits nonzero while valid baseline passes.
  </acceptance_criteria>
  <done>The exact f416-based two-file candidate is locally accounting-complete, fully tested, diff-bound, and ready for explicit signed-commit/push authorization.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>05-02-02: Authorize one exact signed error-funnel commit and protected candidate push.</decision>
  <context>Local evidence proved f416 fixes newline tokenization but silently drops an outer `parse_line` error. The proposed candidate is parent f416 plus only the displayed two-file diff SHA-256 `f9c64bbabd9731ccb68ce8708c64048fe8ae4fe7aff20931062504448d3c1787`; upstream and RateMyPCB tests pass, public API/dependencies/licenses are unchanged, and a665/5ac are excluded. `APPROVE_PUSH` authorizes creation of branch `ratemypcb/gerber-parser-accounting-fix`, one signed commit by configured identity Mattia Fiumara with exact message `fix: retain outer parse errors`, its targeted active protection/ruleset, and one non-force push to exact `refs/heads/ratemypcb/gerber-parser-accounting-fix`. `REFUSE_PUSH` authorizes cleanup of the temporary path dependency/lock and terminal STOP. The human follow-up “Let's do it on a branch indeed, and we can upstream the fix” also authorizes one PR from this exact branch to `MakerPnP/gerber-parser:master` after independent acceptance. Neither authorization permits any other commit/change, force-push, merge, tag, release, publication, or RateMyPCB commit/push.</context>
  <options>
    <option id="APPROVE_PUSH">
      <name>APPROVE_PUSH</name>
      <pros>Creates exactly the tested signed two-file commit and makes it reachable under a protected exact ref for full-rev Cargo review.</pros>
      <cons>Creates a public maintenance ref and monitoring obligation; no future mutation is implied.</cons>
    </option>
    <option id="REFUSE_PUSH">
      <name>REFUSE_PUSH</name>
      <pros>Creates no candidate ref/push and removes temporary candidate state.</pros>
      <cons>FAB-03 remains blocked pending another approved approach.</cons>
    </option>
  </options>
  <resume-signal>Respond with exactly `APPROVE_PUSH` or `REFUSE_PUSH`.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>05-02-03: Dispatch APPROVE_PUSH or REFUSE_PUSH exactly</name>
  <files>Cargo.lock, crates/ratemypcb-core/Cargo.toml, crates/ratemypcb-core/tests/fabrication_release.rs, tests/verify_gerber_fork_candidate.py, tests/test_verify_gerber_fork_candidate.py, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-PUSH-DECISION.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-CANDIDATE.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-REVIEW.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-SUMMARY.md</files>
  <read_first>
    - Raw checkpoint token; accept only exact `APPROVE_PUSH` or `REFUSE_PUSH`.
    - Current temporary path edge/lock and detached f416 working tree with the exact approved two-file diff.
  </read_first>
  <behavior>
    - REFUSE_PUSH performs cleanup, production-import absence check, STOP artifact/summary, and terminates without later checkpoint.
    - APPROVE_PUSH creates only the exact signed commit/branch, ruleset, and non-force push, then full packet/review/dev-git work.
    - No unknown, case-folded, prefixed, or prose response falls through to either outcome.
  </behavior>
  <action>
    Dispatch by exact token equality before any mutation. Any other value aborts unchanged. Write `05-02-PUSH-DECISION.json` with duplicate-safe schema, the exact token, human authority, and timestamp so verification uses the same immutable dispatch value.

    For `REFUSE_PUSH`: remove the temporary path dev entry, regenerate lock without candidate-only entries, verify parser absent from core normal/dev manifest, lock, and production source, write candidate JSON with outcome `REFUSE_PUSH`, recommendation/status STOP, exact f416 identities and timestamp, and write `05-02-SUMMARY.md` recording refusal/cleanup/FAB-03 blocked. End Plan 05-02 immediately; do not run APPROVE logic or adoption checkpoint.

    For `APPROVE_PUSH`: rerun local accounting, exact diff/file hashes, upstream tests, and negative verifier matrix. Create only branch `ratemypcb/gerber-parser-accounting-fix`; stage only `src/parser.rs` and `tests/component_tests.rs`; make one signed commit with parent f416 and exact message `fix: retain outer parse errors`; require its diff still hashes to the approved value and no other path changed. Push once non-force using the resulting full SHA to exact ref, apply approved targeted active ruleset/protection, and verify exact-ref ls-remote/fetch returns that full SHA and tree. Replace the path edge and test constant with exact dev-only git URL/resulting full rev and regenerate lock.

    Create unique archive using `head_sha="$(git -C ../gerber-parser rev-parse HEAD)"; archive="$(mktemp /tmp/ratemypcb-gerber-parser.XXXXXXXX.tar)"; git -C ../gerber-parser archive --format=tar --prefix="gerber-parser-$head_sha/" "$head_sha" &gt; "$archive"`; reproduce to a second unique path and require byte equality. Require nonempty external `RATEMYPCB_UCAMCO_CORPUS`, exact two ZIP digests, and direct parsing from same verified buffers. Run complete compact/error/Route/trust/resource, source/API/dependency/license/advisory, Cargo tree/metadata, governance and corpus evidence.

    Write strict duplicate-free candidate JSON with exact identities/timestamps/archive/ruleset/Cargo/evidence/recommendation, then commission fresh independent read-only review JSON binding candidate SHA-256, repeated identities, ACCEPT/REJECT, and P0/P1/P2 arrays. Any finding/mismatch is STOP. After independent ACCEPT and candidate verifier pass, open one upstream PR from `ratemypcb:ratemypcb/gerber-parser-accounting-fix` to `MakerPnP/gerber-parser:master`, record its URL, and do not merge it. Run the negative matrix again; adoption checkpoint is reachable only if all checks pass.
  </action>
  <verify><automated>set -euo pipefail; token="$(python3 -c 'import json; print(json.load(open(".planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-PUSH-DECISION.json"))["decision"])')"; if test "$token" = REFUSE_PUSH; then ! grep -R -q gerber_parser crates/ratemypcb-core/Cargo.toml crates/ratemypcb-core/src Cargo.lock; grep -q 'REFUSE_PUSH' .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-SUMMARY.md; elif test "$token" = APPROVE_PUSH; then : "${RATEMYPCB_UCAMCO_CORPUS:?set RATEMYPCB_UCAMCO_CORPUS}"; cargo test -p ratemypcb-core --test fabrication_release --locked gerber_adoption_spike_; RATEMYPCB_UCAMCO_CORPUS="$RATEMYPCB_UCAMCO_CORPUS" cargo test -p ratemypcb-core --test fabrication_release --locked gerber_adoption_spike_official_local_checkpoint -- --nocapture; python3 tests/test_verify_gerber_fork_candidate.py; python3 tests/verify_gerber_fork_candidate.py --mode candidate; else exit 1; fi</automated></verify>
  <acceptance_criteria>
    - Exact REFUSE_PUSH cleanup is reachable, complete, production-source-safe, recorded, and terminal.
    - Exact APPROVE_PUSH is the only branch/commit/push path and the committed source equals the approved local diff byte-for-byte.
    - Exact remote ref/full SHA/tree, dev git/full rev Cargo placement, deterministic archive, complete evidence, and fresh review are verified.
    - Candidate verifier and every negative matrix case pass immediately before adoption checkpoint.
  </acceptance_criteria>
  <done>REFUSE_PUSH ends cleanly blocked, or APPROVE_PUSH yields one strictly verified immutable dev-only candidate.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>05-02-04: Adopt or stop the exact verified f416-based fork candidate.</decision>
  <context>Candidate verifier and 11-case negative matrix passed for exact fork URL, release/f416 parent, signed repair head `54004bc52c11699b49cd287a49135380feee86b3`, tree `5a8bddf91cd77b7e6700df0eb1027a4fc231c9a6`, protected/locked ref, deterministic archive, current packet/review hashes, independent ACCEPT with empty P0/P1/P2, exact remote fetch, and dev-only Cargo full SHA. Upstream PR #26 is open and unmerged. The historical token name `PASS_F416` means adopting this exact f416-based head including the reviewed error-funnel commit; it does not mean bare f416. `PASS_F416` authorizes Plan 05-03 to reverify then promote only this identical source for FAB-03. `STOP_F416` authorizes verified cleanup. Neither authorizes another SHA, PR merge, publication, release, or product approval.</context>
  <options>
    <option id="PASS_F416"><name>PASS_F416</name><pros>Permits exact-source production work after strict re-verification.</pros><cons>Accepts fork maintenance and git-only publication block.</cons></option>
    <option id="STOP_F416"><name>STOP_F416</name><pros>Keeps production free of candidate.</pros><cons>FAB-03 and downstream plans remain blocked.</cons></option>
  </options>
  <resume-signal>Respond with exactly `PASS_F416` or `STOP_F416`.</resume-signal>
</task>

<task type="auto">
  <name>05-02-05: Bind exact adoption token</name>
  <files>.planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-FORK-DECISION.json, .planning/phases/05-manufacturing-evidence-model-and-gerber-baseline/05-02-SUMMARY.md</files>
  <read_first>
    - Raw checkpoint token and current candidate/review JSON hashes.
  </read_first>
  <action>Accept only exact `PASS_F416` or `STOP_F416`. Write duplicate-free decision JSON with exact token, human identity, ordered timestamp, repeated URL/ref/base/head/tree/archive, and current candidate/review SHA-256. Update summary with decision but do not clean/promote. Run decision verifier; on failure delete decision JSON and abort.</action>
  <verify><automated>python3 tests/verify_gerber_fork_candidate.py --mode decision</automated></verify>
  <acceptance_criteria>
    - Exact human token is preserved without aliasing/inference.
    - Decision is hash/time/identity bound to current candidate and review.
    - Dev-only placement remains until Plan 05-03 dispatch.
  </acceptance_criteria>
  <done>Plan 05-03 receives one strictly verified exact PASS_F416 or STOP_F416.</done>
</task>

</tasks>

<artifacts_this_phase_produces>

- Safe sibling exact-f416 checkout, one bound non-breaking local error-funnel patch, one approved signed commit/protected exact ref, and one independently accepted upstream PR left unmerged.
- Updated compact/accounting spike and stdlib verifier plus required negative matrix.
- REFUSE_PUSH cleanup STOP, or exact dev-only full-rev candidate with deterministic archive and immutable candidate/review JSON.
- Exact human decision JSON; no production code, repair, publication, or release.
</artifacts_this_phase_produces>

<verification>
Local f416 RED evidence, exact two-file repair, full tests, and negative matrix precede the signed commit/push checkpoint. Post-checkpoint task dispatches exact APPROVE_PUSH/REFUSE_PUSH, with REFUSE terminal cleanup and APPROVE immutable packet work. Candidate mode plus negative matrix are the final checks before PASS_F416/STOP_F416; decision mode binds the human token afterward.
</verification>

<acceptance_exit_criteria>

- Fork authorization and both checkpoint token contracts are exact and non-overlapping.
- REFUSE_PUSH cleanup is executable and terminal.
- Exact f416 plus only the approved non-breaking error-funnel commit passes local accounting, remote identity, Cargo, archive, corpus/resource/supply-chain, review, and negative verifier evidence.
- Human PASS_F416/STOP_F416 is current and immutable; production remains untouched.
</acceptance_exit_criteria>

<honest_deferrals>
Production FAB-03 is Plan 05-03. FAB-04/05 foundation is Plan 05-04, FAB-06 integration Plan 05-05, FAB-08 closure Plan 05-06. ODB++/IPC-2581 and all Phase 6-8/legal/corpus deferrals remain unchanged.
</honest_deferrals>

<output>Create `05-02-SUMMARY.md`; historical `05-02-DEPENDENCY-CHECKPOINT.md` is never overwritten.</output>
