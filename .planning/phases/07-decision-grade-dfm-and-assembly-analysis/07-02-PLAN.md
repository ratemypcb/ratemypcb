---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - crates/ratemypcb-core/src/dfm.rs
  - crates/ratemypcb-core/src/lib.rs
  - crates/ratemypcb-core/tests/dfm_release.rs
  - tests/fixtures/dfm/population-targets.json
  - tests/fixtures/dfm/prerequisite-mutations.json
autonomous: true
requirements: [DFM-03, DFM-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Human authorization permits this format-independent tracer against the accepted Phase 5 native KiCad and Gerber/X2+Excellon baseline; Phase 6 remains required only for ODB++/IPC-2581 integration and parity fixtures."
    - "D-06 is proven by one production-quality `assembly.population-parity.v1` tracer that consumes existing typed occurrence-first `SchematicReview.mismatches` through qualification/evidence mapping, finalization, report validation, and approval recomputation."
    - "Population/fitted/DNP/quantity/placement/footprint/revision comparisons and identity fallback remain owned by schematic reconciliation; `dfm.rs` does not duplicate or alter them."
    - "D-04/D-07 keep the tracer not_checked on incomplete reconciliation authority and EvidenceOnly until qualification/promotion policy explicitly permits otherwise."
  artifacts:
    - path: "crates/ratemypcb-core/src/dfm.rs"
      provides: "Single plain Phase 7 core module and population-parity tracer"
    - path: "crates/ratemypcb-core/tests/dfm_release.rs"
      provides: "End-to-end positive, hard-negative, mutation, provenance, and approval checks"
  key_links:
    - from: "crates/ratemypcb-core/src/schematic.rs"
      to: "crates/ratemypcb-core/src/dfm.rs"
      via: "existing typed SchematicReview.mismatches and reconciliation capability/artifact metadata"
    - from: "crates/ratemypcb-core/src/lib.rs"
      to: "crates/ratemypcb-core/src/dfm.rs"
      via: "one mapping call after schematic reconciliation and before finalize_evidence"
---

<objective>
Land one production-quality format-independent deterministic population tracer end to end against the accepted Phase 5 baseline.

Purpose: Prove the complete D-01/D-02/D-03/D-04/D-06/D-07 architecture before analyzer breadth.
Output: One plain core module that maps existing typed schematic reconciliation into an EvidenceOnly population family integrated with existing evidence/report policy.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@crates/ratemypcb-core/src/fabrication.rs
@crates/ratemypcb-core/src/schematic.rs
@crates/ratemypcb-core/src/lib.rs
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Trace one population mismatch from source rows to validated evidence</name>
  <precondition>Human authorization covers this format-independent slice against existing typed schematic reconciliation plus accepted native KiCad and Gerber/X2+Excellon; do not add or claim ODB++/IPC-2581 integration or parity.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/population-targets.json</files>
  <read_first>.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/schematic.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-core/tests/schematic_release.rs; tests/fixtures/dfm/manifest.json</read_first>
  <behavior>- An existing typed schematic reconciliation mismatch in the population subset (`board-population`, `bom-population`, `bom-quantity`, `bom-fitted`, `dnp`, `placement-population`, or `revision`) maps to one `assembly.population-parity.v1` EvidenceOnly finding using the existing mismatch location/join plus schematic artifact identity.
- A completed reconciliation with no population-subset mismatch maps to semantic pass evidence without a finding; incomplete, ambiguous, or invalid reconciliation authority remains not_checked.
- Reordering identical inputs preserves the existing reconciliation result and the mapped family evidence ID.
- The mapping occurs after canonical manufacturing/native and schematic reconciliation, before `finalize_evidence`.</behavior>
  <action>Start with a failing end-to-end test, then add only `mod dfm` and one plain `dfm.rs`. Consume the already-produced typed `SchematicReview.mismatches`, reconciliation capability, occurrences, and artifact digests from `schematic.rs`; map only the named population subset to stable family `assembly.population-parity.v1`. Do not parse BOM/placement rows or reimplement population, fitted/DNP, quantity, placement, footprint, revision, occurrence identity, exact-UUID join, or unique-reference fallback behavior in `dfm.rs`. Preserve the existing mismatch field, expected/actual, join/confidence, and source location in evidence; if the typed output lacks authority required for a stronger claim, emit not_checked rather than inventing row provenance. Add only DFM family qualification/evidence mapping and feed ordinary `Finding`/`Coverage` into existing finalization and validation. Keep GateImpact EvidenceOnly. Use only the accepted Phase 5 native KiCad and Gerber/X2+Excellon baseline; leave ODB++/IPC-2581 integration and parity fixtures explicitly blocked on Phase 6 and its human decision.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release population_tracer --locked</automated></verify>
  <acceptance_criteria>The real `review()` path emits source-linked stable evidence for one seeded mismatch; identical reordered inputs produce identical evidence IDs; the report validator accepts the output; no adapter, viewer, schema, approval engine, or second board representation is changed.</acceptance_criteria>
  <done>The production population tracer works end to end and remains EvidenceOnly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail the tracer closed across every prerequisite and provenance mutation</name>
  <precondition>Human authorization covers fail-closed tests for the same format-independent tracer; ODB++/IPC-2581 integration and parity remain blocked.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/prerequisite-mutations.json</files>
  <read_first>crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-core/tests/fabrication_release.rs; crates/ratemypcb-core/tests/dfm_release.rs; tests/fixtures/dfm/prerequisite-mutations.json</read_first>
  <behavior>- Missing, partial, failed, stale, duplicated, conflicted, dangling, or otherwise incomplete typed reconciliation authority produces not_checked/attention and never pass or Blocking.
- Removing existing mismatch/artifact source provenance causes validation failure, not an evidence record with an empty or guessed location.
- Existing exact-UUID and unique-reference fallback behavior is unchanged under every mutation.
- No package compatibility, paste coverage, construction default, or net-name claim can enter this family.</behavior>
  <action>Expand the tracer mutation loop using the inert manifest and mutate inputs through the existing schematic reconciliation path; assert the DFM mapper consumes the resulting typed output rather than recomputing expected/actual facts. Add mapper-local guards only for reconciliation completion and required provenance, ensure no `None` semantic result becomes pass, and reject forged Blocking on the unqualified family in `validate_report`; do not rely on serde's default-blocking `GateImpact`. Preserve existing schematic comparison/fallback semantics, evidence finalization, and approval recomputation per D-01/D-07.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release population_ --locked &amp;&amp; cargo test -p ratemypcb-core --test schematic_release reconciliation_ --locked</automated></verify>
  <acceptance_criteria>All positive, hard-negative, and mutation cases pass; every mutation is outside TP/FP/FN/TN; no mutation improves approval; no ODB++/IPC-2581 integration, fixture, or support claim is added.</acceptance_criteria>
  <done>The single tracer is production-quality, fail-closed, deterministic, and source-linked.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| BOM/placement/schematic/fabrication facts → core analyzer | Bounded but untrusted local design facts cross into release policy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-03 | Tampering | capability/occurrence identity | high | mitigate | Existing dispatcher plus duplicate, omission/conflict, and dangling-provenance guards force not_checked. |
| T-07-04 | Elevation of Privilege | GateImpact selection | high | mitigate | Analyzer cannot set Blocking; report validation rejects unqualified impact. |
| T-07-05 | Denial of Service | placement/BOM rows | medium | mitigate | Retain existing bounded row counts, checked decimal conversion, deterministic sets/maps. |
| T-07-SC | Tampering | package installs | low | accept | No package install or new dependency exists. |
</threat_model>

<requirements_coverage>

- DFM-03: population/BOM/placement occurrence tracer with source locations.
- DFM-05: positive, hard-negative, mutation, metric, and promotion-safe behavior for the first family.
</requirements_coverage>

<artifacts_this_phase_produces>

- `dfm.rs`, the only Phase 7 production module.
- EvidenceOnly `assembly.population-parity.v1` mapping over existing typed schematic reconciliation, integrated into the existing report path without duplicated comparisons.
</artifacts_this_phase_produces>

<verification>
Focused DFM and schematic integration tests pass; `cargo fmt --all -- --check`; no files under CLI viewer assets are modified.
</verification>

<success_criteria>
One format-independent deterministic family proves all layers against the accepted Phase 5 baseline, reuses schematic reconciliation as the single comparison authority, and cannot pass/block on unqualified inputs; ODB++/IPC-2581 parity remains blocked.
</success_criteria>

<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-02-SUMMARY.md` when done.</output>
