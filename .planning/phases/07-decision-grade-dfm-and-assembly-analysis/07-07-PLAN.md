---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 07
type: execute
wave: 7
depends_on: [07-06]
files_modified: [crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/construction-targets.json]
autonomous: true
requirements: [DFM-02, DFM-05]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Construction/order families consume Plan 07-03's production-normalized source/version-bound customer order/profile authority and emit match/conflict only for facts represented by existing ConstructionEvidence or semantically matching constraints (D-04/D-05)."
    - "Unrepresented drill-span/plating, castellation/edge-plating, or profile acknowledgements defer their match/conflict subfamily and emit source-linked confirmation gaps only; no `Other` encoding, model extension, or common-practice default is allowed."
    - "Stackup, material/thickness, finish, impedance, special process, and every explicit gap are qualification-gated (D-01/D-07)."
  artifacts:
    - {path: "tests/fixtures/dfm/construction-targets.json", provides: "Construction match/conflict/gap and mutation corpus"}
    - {path: "crates/ratemypcb-core/src/dfm.rs", provides: "Five deterministic confirmation families"}
  key_links:
    - {from: "07-03 production authority seam", to: "construction families", via: "source-linked represented ConstructionEvidence/ManufacturingConstraint facts plus explicit unrepresented confirmation gaps through the sequential dependency chain"}
    - {from: "construction families", to: "FabricationReview construction/constraints/layers/tools/profile", via: "exact represented facts, authority and provenance"}
---

<objective>
Deliver explicit construction, profile, and order confirmation evidence without fabricated defaults.

Purpose: Complete DFM-02 using the same dispatcher, qualification, evidence finalization, and unblock ordering.
Output: Five confirmation families and one focused corpus, with match/conflict limited to existing represented facts and every other requested fact surfaced only as a confirmation gap.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-03-SUMMARY.md
@tests/fixtures/dfm/declarations.json
@crates/ratemypcb-core/src/fabrication.rs
@crates/ratemypcb-core/src/dfm.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Reconcile stackup order, material, and thickness</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/construction-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/stackup.rs; tests/fixtures/dfm/manifest.json</read_first>
  <behavior>- `dfm.stackup-order-confirmation.v1` and `dfm.total-thickness-material.v1` distinguish exact match, conflict, and missing-authority confirmation gap.
- Thickness uses picometres and retains both authorities; material compares only explicit declared strings.
- Filename layer counts and legacy f64 Stackup are never authority.
- Match/conflict cases exercise Plan 07-03's real order/profile declaration normalization into existing construction/constraint fields; fixture-only injected requirements are not production authority.</behavior>
  <action>Implement two confirmation families from existing `ConstructionEvidence`, `LayerOrder`, semantically matching constraints, and the explicit requirement provenance normalized by Plan 07-03. Compare layer order/material/thickness/total thickness only where both design and customer authority are represented; otherwise emit a confirmation gap. Route absent/stale/duplicate facts through not_checked/attention and central EvidenceOnly/Blocking selection. Add production-seam positive/conflict, hard-negative, direct-injection/no-default, and standard mutation cases per D-04/D-05/D-07.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release construction_stackup_ --locked</automated></verify>
  <acceptance_criteria>Production-seam match/conflict/gap, exact units, authority disagreement, missing/unrepresented order/material/thickness, stale/duplicate/direct-injected authority, and filename-inference mutations pass; no missing or unrepresented fact creates pass or violation.</acceptance_criteria>
  <done>Stackup/material/thickness are explicit evidence, conflicts, or confirmation gaps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit drill-span and plating confirmation gaps without extending the model</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/construction-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/dfm.rs; tests/fixtures/dfm/construction-targets.json</read_first>
  <behavior>- `dfm.drill-span-plating.v1` reports canonical design-side tool/hole span and plating evidence, but current existing construction/constraint fields cannot represent the customer drill-span/plating acknowledgement needed for an authoritative match/conflict.
- Therefore every order-comparison result in this slice is a source-linked confirmation gap; the match/conflict subfamily is explicitly deferred.
- Unknown/mixed design plating, absent/ambiguous span, missing order input, or unrepresented acknowledgement cannot pass/block, and through-hole is never assumed.</behavior>
  <action>Reuse canonical tools/drills/plating/spans and the Plan 07-03 declaration provenance to emit deterministic per-tool confirmation gaps only. Do not encode drill-span/plating authority as `ConstraintKind::Other`, add a field/model, parse formats in the analyzer, or compare unrepresented values. Add qualification targets for present-but-unrepresented acknowledgement, absent authority, unknown/mixed design facts, duplicates, stale/partial records, and mutations; all remain not_checked/EvidenceOnly attention until a separately approved canonical representation exists.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release construction_drill_span_ --locked</automated></verify>
  <acceptance_criteria>Present-but-unrepresented, unknown/mixed/absent/duplicate/stale and layer-order mutation cases all produce deterministic source-linked confirmation gaps; no match, conflict, pass, or Blocking result is emitted.</acceptance_criteria>
  <done>Drill span/plating design evidence is visible and customer-order comparison remains honestly deferred.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile finish/profile and impedance/special-process acknowledgements</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/construction-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/dfm.rs; tests/fixtures/dfm/construction-targets.json</read_first>
  <behavior>- `dfm.finish-profile.v1` may match/conflict explicit finish because it is represented; profile/castellation/edge-plating acknowledgements are currently unrepresented and produce confirmation gaps only.
- `dfm.impedance-special-process.v1` may match/conflict explicit impedance and special-process constraints normalized to existing semantic kinds; missing or unrepresented acknowledgements produce gaps.
- No impedance calculation, `Other`-kind substitution, model extension, or construction default is introduced.</behavior>
  <action>Implement explicit match/conflict/gap outputs only for represented finish, impedance, and special-process facts from Plan 07-03's production normalization. Preserve source/version and both compared locations. For profile/castellation/edge-plating or any other unrepresented acknowledgement, emit only a source-linked confirmation gap and defer match/conflict. Run central qualification and smallest-unblock policy; add production-seam fixture classes and a DFM-02 matrix that distinguishes represented comparisons from deferred gaps.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release construction_ --locked</automated></verify>
  <acceptance_criteria>All five DFM-02 family IDs have positive/hard-negative/mutation metrics; represented finish/impedance/special-process cases use the production authority seam; unrepresented profile/castellation/edge-plating and missing authority rank only as release confirmation gaps; no impedance calculation, direct-fixture authority, or common-practice inference exists.</acceptance_criteria>
  <done>DFM-02 is complete as explicit source-linked confirmation evidence.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| design construction facts ↔ order/profile authority | Missing or conflicting declarations could be fabricated into compliance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-15 | Spoofing | construction defaults | high | mitigate | Exact source/version authority required; gaps are visible not_checked attention. |
| T-07-16 | Tampering | units/material strings | medium | mitigate | Checked fixed-point units and exact declared strings with dual provenance. |
| T-07-SC | Tampering | installs | low | accept | No new dependency. |
</threat_model>
<requirements_coverage>- DFM-02: all requested construction/profile/order evidence, conflicts, and gaps.

- DFM-05: per-family fixtures, metrics and promotion policy.</requirements_coverage>
<artifacts_this_phase_produces>- Five construction confirmation families and their corpus.</artifacts_this_phase_produces>
<verification>Focused construction suite, fmt, clippy and `git diff --check` pass.</verification>
<success_criteria>Every DFM-02 match/conflict uses production-normalized existing represented authority; every missing or unrepresented fact is an honest confirmation gap.</success_criteria>
<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-07-SUMMARY.md` when done.</output>
