---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 09
type: execute
wave: 9
depends_on: [07-08]
files_modified: [crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-cli/tests/decision_report.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json, tests/fixtures/dfm/declarations.json]
autonomous: true
requirements: [DFM-03, DFM-04, DFM-05]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Plan 07-03's existing CLI→ReviewOptions→core declaration seam is extended in place with bounded source-linked process/probe/electrical/thermal/interface intent; no second flag, parser, input model, or constructor migration is added, and fixtures do not bypass production normalization (D-01/D-02)."
    - "Assembly access and test-point access expose explicit process/probe assumptions and are EvidenceOnly until exact family-specific human approval (D-04/D-08)."
    - "Missing, stale, duplicate, partial, or unbounded declarations, target-net authority, connectivity, placement, or geometry yield not_checked; reference/net-name heuristics are forbidden (D-05)."
  artifacts:
    - {path: "tests/fixtures/dfm/declarations.json", provides: "Inference-intent extension of the source-linked bounded declaration input created by Plan 07-03"}
    - {path: "crates/ratemypcb-core/src/dfm.rs", provides: "In-place declaration extensions and assembly access/testpoint conservative-inference families"}
    - {path: "tests/fixtures/dfm/assembly-targets.json", provides: "Inference positive/hard-negative/mutation targets"}
  key_links:
    - {from: "07-03 production declaration seam", to: "assembly inference declarations", via: "extend the same optional DfmDeclarations value and CLI path"}
    - {from: "assembly inference", to: "07-PROMOTION-CHECKPOINT.md", via: "exact family/version human approval gate, closed by default"}
---

<objective>
Close DFM-03's access/test-point scope as bounded conservative inference.

Purpose: Extend the already-proven source-linked declaration seam with only the intent needed here, then surface useful attention without fabricating process or electrical intent.
Output: One in-place bounded declaration extension and two EvidenceOnly inference families with explicit assumptions and full qualification reporting.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-03-SUMMARY.md
@crates/ratemypcb-core/src/dfm.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend the existing bounded declarations with inference intent</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-cli/tests/decision_report.rs, tests/fixtures/dfm/declarations.json</files>
  <read_first>The canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-03-SUMMARY.md; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-cli/tests/decision_report.rs; tests/fixtures/dfm/declarations.json; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md</read_first>
  <behavior>- The optional local declaration file and `ReviewOptions` field created in Plan 07-03 remain the only input path, preserving source path, digest, structural location, producer/version, declaration version, and explicit applicability; absent input leaves all dependent families not_checked.
- The in-place bounded schema extension carries named process/tool and probe envelopes; stable target-net IDs; signal edge-rate/frequency and reference-plane/discontinuity limits; current plus source-declared minimum copper/process envelope; voltage/creepage/material/environment/coating rule; pair/impedance/skew limits; power plus source-declared copper/via/boundary envelope; and protocol/connector/pin constraints.
- Unknown fields/version/model, duplicate authority, stale/partial declarations, non-finite/out-of-range values, dangling canonical IDs, missing units, or unbounded collections are rejected or marked unqualified before analysis.</behavior>
  <action>Extend the existing optional `DfmDeclarations` type and `tests/fixtures/dfm/declarations.json` from Plan 07-03 with only the bounded inference records named above. Reuse the existing `--dfm-declarations` parser, byte digest, provenance, collection/range limits, duplicate-authority rejection, `ReviewOptions` field, and core convergence point; do not add another flag, parser, constructor field, declaration root, board model, plugin, approval engine, or viewer policy. Every engineering comparison is bounded by a named source/version model and explicit numeric limits: Phase 7 measures geometry against declared envelopes and does not invent ampacity, field, impedance, or thermal simulation. Add a real CLI report test proving the extended records reach core and a names-only control remains not_checked.</action>
  <verify><automated>cargo test -p ratemypcb-cli --test decision_report --locked dfm_inference_declarations_ &amp;&amp; cargo test -p ratemypcb-core --test dfm_release declaration_ --locked</automated></verify>
  <acceptance_criteria>The existing real CLI path accepts one valid source-linked bounded inference extension, preserves its digest/locations/model versions, rejects malformed/duplicate/stale/unbounded cases, and absent input produces not_checked rather than a fixture-only synthetic result; Plan 07-03 remains the sole `ReviewOptions`/CLI input seam and constructor migration owner.</acceptance_criteria>
  <done>Every inference family has a production input seam and an explicit bounded comparison contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Report assembly access risk only against a declared process envelope</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json</files>
  <read_first>The canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/fabrication.rs; tests/fixtures/dfm/declarations.json; tests/fixtures/dfm/assembly-targets.json</read_first>
  <behavior>- `assembly.access.v1` lists process/tool envelope assumptions and compared source locations.
- Missing placement/profile/component geometry or named envelope is not_checked.
- Complete inputs produce EvidenceOnly attention/clean observation; no current approval can select Blocking.</behavior>
  <action>Implement one bounded inference family using explicit process/tool-envelope facts only. Keep the inference label visible in finding evidence, route prerequisites through dispatch and impact through the inference-specific human gate per D-08. Add positive, hard-negative, missing-assumption, mutation, metrics and removed-approval tests.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release assembly_access_ --locked</automated></verify>
  <acceptance_criteria>Declared-envelope cases are deterministic; missing/unknown assumptions are not_checked; forged approval/blocking and removed checkpoint entries downgrade to EvidenceOnly.</acceptance_criteria>
  <done>Assembly access is useful conservative evidence, never implicit process truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Report test-point access only with declared targets and probe envelope</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json</files>
  <read_first>The canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/lib.rs; tests/fixtures/dfm/declarations.json; tests/fixtures/dfm/assembly-targets.json</read_first>
  <behavior>- `assembly.testpoint-access.v1` requires complete connectivity/component/pin/placement/profile geometry, explicit target-net authority, and probe/process envelope.
- TP-like references or net names cannot establish test intent.
- All complete observations remain EvidenceOnly with assumptions shown.</behavior>
  <action>Implement exact envelope comparison over explicitly declared target identities. Do not reuse the existing TP reference exclusion as intent authority and do not pattern-match net names per D-05. Add corpus/metrics/mutations and a DFM-03 matrix proving population, side/rotation, paste, courtyard, footprint, access and testpoint coverage.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release testpoint_access_ --locked &amp;&amp; cargo test -p ratemypcb-core --test dfm_release dfm03_family_matrix --locked</automated></verify>
  <acceptance_criteria>Explicit-target cases, absent connectivity/envelope/target, TP-reference and net-name hard negatives, standard mutations and EvidenceOnly checkpoint behavior pass; all DFM-03 families are represented.</acceptance_criteria>
  <done>DFM-03 is complete with deterministic families and bounded EvidenceOnly access inference.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| design labels/geometry → process inference | Names or absent process inputs could spoof assembly/test intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-19 | Spoofing | process/test intent | high | mitigate | Explicit envelope and target authority; names never qualify. |
| T-07-20 | Elevation of Privilege | inference promotion | high | mitigate | Exact family/version human checkpoint; closed means EvidenceOnly. |
| T-07-SC | Tampering | installs | low | accept | No new dependency. |
</threat_model>
<requirements_coverage>- DFM-03: access and test-point risks plus complete assembly matrix.

- DFM-04: inference assumptions/capabilities and labeling.
- DFM-05: full corpus/metrics/mutations.</requirements_coverage>
<artifacts_this_phase_produces>- One in-place extension of the Plan 07-03 production declaration seam and two bounded assembly inference families, EvidenceOnly by default.</artifacts_this_phase_produces>
<verification>Focused access/testpoint tests and DFM-03 matrix pass; checkpoint remains closed unless an exact human entry exists.</verification>
<success_criteria>DFM-03 is complete without inferred process or net intent and without unapproved inference blocking.</success_criteria>
<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-09-SUMMARY.md` when done.</output>
