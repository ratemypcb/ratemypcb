---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 05
type: execute
wave: 5
depends_on: [07-04]
files_modified: [crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/fabrication/native.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/geometry-targets.json]
autonomous: true
requirements: [DFM-01, DFM-05]
estimate: {tokens: 28000, raw_tokens: 28000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Copper-to-edge, different-net clearance, and annular ring report exact fixed-point measurements with both compared source facts only after their source/version-bound thresholds arrive through Plan 07-03's production authority seam (D-01)."
    - "Native KiCad emits a policy-free canonical pad↔hole ownership fact from one authoritative pad object; every path without that authority yields not_checked instead of nearest-hole association (D-01/D-04/D-05)."
    - "Candidate generation is deterministic and bounded under Phase 5's feature ceiling."
  artifacts:
    - {path: "crates/ratemypcb-core/src/dfm.rs", provides: "Copper-edge, copper-clearance, and annular-ring families"}
    - {path: "tests/fixtures/dfm/geometry-targets.json", provides: "Positive/hard-negative/mutation targets and metrics"}
  key_links:
    - {from: "07-03 production authority seam", to: "distance-family thresholds", via: "fixed-point ManufacturingConstraint and provenance through the 07-04→07-05 dependency chain"}
    - {from: "distance candidates", to: "FabricationReview features/connectivity/profile/tools", via: "layer-local exact fixed-point pruning and provenance"}
---

<objective>
Add the three exact distance families after outline topology is proven.

Purpose: Complete the high-value measured copper geometry core without guessing unsupported associations.
Output: Copper-edge, different-net clearance, and annular-ring families with bounded deterministic qualification.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-03-SUMMARY.md
@tests/fixtures/dfm/declarations.json
@crates/ratemypcb-core/src/fabrication.rs
@crates/ratemypcb-core/src/dfm.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Measure copper to exterior, cutout, and routed edges</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/geometry-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/dfm.rs; tests/fixtures/dfm/geometry-targets.json</read_first>
  <behavior>- `dfm.copper-edge.v1` reports nearest exact boundary distance, copper/profile feature IDs, threshold/delta and both locations.
- Exterior, cutout and routed boundaries are distinct; absent threshold or unresolved profile/polarity/transform is not_checked.
- Exact threshold is clean and one source-resolution below is a violation.
- Threshold-bearing positive/boundary cases exercise Plan 07-03's declaration normalizer; fixture-only, anonymous-preset, missing, duplicate, stale, or inapplicable authority is not_checked.</behavior>
  <action>Reuse the qualified outline topology and add only exact represented-shape boundary distance. Use layer-local deterministic pruning, checked arithmetic, and deadline checks. Obtain every production threshold through the Plan 07-03 `DfmDeclarations`→`ManufacturingConstraint` path and add direct-injection/preset controls that cannot qualify. Do not use extents/bounding boxes as the final distance or omit routed slots. Add corpus classes/mutations and route final impact through central policy per D-07.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release copper_edge_ --locked</automated></verify>
  <acceptance_criteria>Exterior/cutout/routed positive and hard-negative cases use the production authority seam; unsupported shapes, missing/invalid/direct-injected/preset authority, overflow/resource, source-link, stable-order, and mutation cases pass fail-closed.</acceptance_criteria>
  <done>Copper-edge measurements are exact, bounded, deterministic, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure different-net copper clearance only with complete connectivity</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/geometry-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/tests/dfm_release.rs</read_first>
  <behavior>- `dfm.copper-clearance.v1` compares only proven different-net features on the same physical layer and reports nearest exact boundary distance.
- Same-net is a hard negative; absent/partial connectivity, zone/region/arc/polarity/transform gaps, or threshold absence is not_checked.
- Pair order and input order cannot affect target identity or tie ordering.
- The clearance threshold is a normalized Plan 07-03 source-linked constraint; fixture-only, anonymous-preset, missing, duplicate, stale, or inapplicable authority is not_checked.</behavior>
  <action>Add deterministic layer/net buckets and bounded candidate pruning; never globally scan one million features pairwise. Reuse existing feature IDs, connectivity, exact geometry, dispatch, qualification, and evidence finalization, and obtain the threshold only through the production declaration normalizer. Add controls proving direct fixture constraints and presets cannot qualify. Do not derive electrical intent from net names per D-05.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release copper_clearance_ --locked</automated></verify>
  <acceptance_criteria>Different-net positives and exact-threshold boundaries use the production authority path; same-net, adjacent-layer, missing/invalid/direct-injected/preset authority, unqualified connectivity, represented-shape and mutation cases pass with deterministic minimum/ties and bounded work.</acceptance_criteria>
  <done>Clearance evidence is exact and never inferred from names or incomplete connectivity.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize authoritative native pad ownership and measure annular ring</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/fabrication/native.rs, crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json, tests/fixtures/dfm/geometry-targets.json</files>
  <read_first>The canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/fabrication/native.rs; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/tests/fabrication_release.rs; schemas/report-2.0.json</read_first>
  <behavior>- Policy-free native normalization emits one canonical pad↔hole ownership record only when the same native KiCad pad object supplies copper pad identity, drill/slot identity, plating, applicable layer span, exact geometry, and source provenance.
- `dfm.annular-ring.v1` reports minimum radial copper around that actual hole boundary on each applicable span layer with pad/hole/tool identities.
- Gerber/XNC/package facts without exact source identity, NPTH, slots, unknown span/plating, incomplete pad geometry, nominal-diameter-only arithmetic, or a normalized annular threshold are not_checked.</behavior>
  <action>Add the smallest canonical `PadHoleAssociation` fact to `FabricationReview` and populate it policy-free in `crates/ratemypcb-core/src/fabrication/native.rs` from same-object native KiCad pad semantics only; complete capability requires unique IDs, applicable copper layers, plating/span, exact pad and hole geometry, and dual provenance. Never join package/XNC facts by proximity, dimensions, names, or nearest geometry. Consume that fact in `dfm.annular-ring.v1`, measure every applicable layer, and obtain its threshold only through Plan 07-03's production declaration normalizer. Route prerequisites/impact through existing dispatch and qualification. Update the exhaustive fabrication fixture and regenerate `schemas/report-2.0.json` from authoritative `report_schema()` for the additive serialized fact. Add authoritative native positives, production-seam exact-threshold hard negatives, direct-injection/preset controls, and mutations for absent/duplicate/dangling association or threshold authority, NPTH, slot, span/plating, unsupported geometry, and reordered input. Preserve finished-hole/tool semantic provenance.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release annular_ring_ --locked &amp;&amp; cargo test -p ratemypcb-core --test fabrication_release native_ --locked &amp;&amp; cargo test -p ratemypcb-core --test fabrication_release model_generated_schema_is_semantically_and_byte_equal --locked</automated></verify>
  <acceptance_criteria>Real native normalization plus the production authority seam produces measured annular-ring evidence from authoritative same-pad ownership; no-go/native baseline exercises a production positive; every non-authoritative package/Gerber/XNC or missing/invalid/direct-injected/preset threshold path is deterministically not_checked; proximity, NPTH, slot, span/plating, unsupported-pad, and mutation cases never pass/block; the exhaustive fabrication fixture compiles and generated/checked-in report schemas are byte-equal.</acceptance_criteria>
  <done>Annular ring is delivered for authoritative native facts and fails closed for every weaker association path.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| high-cardinality geometry → pairwise measurements | Hostile inputs can induce overflow, false associations, or unbounded work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-11 | Denial of Service | geometry pairing | high | mitigate | Layer-local deterministic pruning, checked bounds, deadline checks; no global O(n²). |
| T-07-12 | Spoofing | net/hole association | high | mitigate | Complete connectivity and explicit association only; names/proximity are not authority. |
| T-07-SC | Tampering | installs | low | accept | No new dependency. |
</threat_model>
<requirements_coverage>- DFM-01: measured clearance, annular ring, and copper-edge.

- DFM-05: per-family corpus, metrics, and fail-closed qualification.</requirements_coverage>
<artifacts_this_phase_produces>- Exact distance-family observations and qualification targets.</artifacts_this_phase_produces>
<verification>All three focused filters, fmt, and clippy on core pass.</verification>
<success_criteria>Distance findings are exact, source-linked, bounded, and absent where current authority is insufficient.</success_criteria>
<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-05-SUMMARY.md` when done.</output>
