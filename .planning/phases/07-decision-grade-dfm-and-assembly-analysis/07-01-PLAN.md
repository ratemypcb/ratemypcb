---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ratemypcb-core/tests/dfm_release.rs
  - tests/fixtures/dfm/manifest.json
  - tests/fixtures/dfm/population-targets.json
  - tests/fixtures/dfm/prerequisite-mutations.json
autonomous: true
requirements: [DFM-05]
estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "D-03 permits this inert fixture/contract slice before Phase 6 because it enables no production analyzer, required coverage, blocking impact, or inference promotion."
    - "D-04/D-07 qualification distinguishes executable positive and hard-negative targets from prerequisite mutations and reports TP, FP, FN, TN, precision, and recall without counting not_checked as TN."
    - "D-05 forbidden inference sources are represented as rejected contract inputs, not guessed facts."
  artifacts:
    - path: "tests/fixtures/dfm/manifest.json"
      provides: "Stable family/version, prerequisite, class, and promotion contract for DFM-01..DFM-04"
    - path: "crates/ratemypcb-core/tests/dfm_release.rs"
      provides: "Inert manifest/metric validator with no production analyzer import"
  key_links:
    - from: "crates/ratemypcb-core/tests/dfm_release.rs"
      to: "tests/fixtures/dfm/manifest.json"
      via: "serde_json fixture loading and contract assertions"
---

<objective>
Freeze the safe, inert qualification contract and first project-authored population corpus without integrating production analysis.

Purpose: Make D-01/D-03/D-04/D-05/D-07/D-08 executable before the Phase 6 dependency opens, while keeping this slice independently reviewable and removable.
Output: One manifest, target/mutation data, and one focused Rust integration-test lane.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze and semantically verify the inert family/corpus contract</name>
  <files>tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/population-targets.json, tests/fixtures/dfm/prerequisite-mutations.json, crates/ratemypcb-core/tests/dfm_release.rs</files>
  <read_first>.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md; tests/fixtures/fabrication/package/manifest.json; tests/fixtures/kicad/mismatch/manifest.json; crates/ratemypcb-core/tests/fabrication_release.rs; crates/ratemypcb-core/tests/schematic_release.rs</read_first>
  <action>Create the three project-authored JSON fixtures and one standalone integration test using existing serde_json. The manifest must enumerate every stable family/version named by research, deterministic versus inference class, exact `CapabilityId` prerequisites, explicit intent/threshold authority, target-key fields `(familyId, familyVersion, fixtureDigest, canonicalTargetIds)`, and promotion state. Add positive, hard-negative, and fail-closed mutation records for `assembly.population-parity.v1`; mutation expected status is `not_checked`, separate from confusion counts. Encode explicit rejection reasons for distributor order packaging, layer-presence-only paste claims, common-practice construction defaults, and net-name intent per D-05. Mark every family EvidenceOnly, especially inference per D-08. In Rust, parse all three files, assert family/version uniqueness, exact nonempty prerequisites and authority metadata, every forbidden-source rejection, EvidenceOnly defaults, stable SHA-256-shaped fixture digests, and complete family coverage. Compute TP/FP/FN/TN by target key, leave precision undefined when TP+FP is zero, leave recall undefined when TP+FN is zero, report mutation/unsupported counts separately, assert the existing 0.95 precision policy, and invent no recall threshold. Keep the lane inert: do not import/call a Phase 7 production analyzer or alter required coverage, GateImpact, report validation, source, schemas, viewer, or approval.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release --locked</automated></verify>
  <acceptance_criteria>The focused semantic test—not JSON syntax alone—fails on duplicate/missing family versions, empty or wrong prerequisites, any absent forbidden-source rejection, any non-EvidenceOnly default, malformed digests, undefined blocking denominators, a synthetic precision below 95%, mutation records counted as TN, or removal of a required family/version; the valid fixtures pass.</acceptance_criteria>
  <done>A runnable semantic qualification contract exists as the separable pre-Phase-6 slice.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| JSON fixtures → test evaluator | Untrusted/mutated corpus metadata is parsed and counted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-01 | Tampering | fixture identity/counts | medium | mitigate | Reject duplicate keys, malformed digests, unknown labels, and inconsistent totals in `dfm_release.rs`. |
| T-07-02 | Spoofing | promotion metadata | high | mitigate | Fixture state cannot enable production GateImpact; all entries start EvidenceOnly and no runtime reader is added. |
| T-07-SC | Tampering | package installs | low | accept | No package install or new dependency exists. |
</threat_model>

<requirements_coverage>

- DFM-05: establishes the adjudication classes, confusion math, undefined-denominator behavior, and mutation accounting used by every later family.
</requirements_coverage>

<artifacts_this_phase_produces>

- Inert family/qualification manifest and project-authored population target/mutation fixtures.
- Focused `dfm_release` contract lane; no production analyzer or policy integration.
</artifacts_this_phase_produces>

<verification>
`cargo test -p ratemypcb-core --test dfm_release --locked` passes and `git diff --name-only` for this plan contains only the four declared test/fixture paths.
</verification>

<success_criteria>
The contract is executable before Phase 6, all inference is EvidenceOnly, and no production behavior or second policy engine exists.
</success_criteria>

<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-01-SUMMARY.md` when done.</output>
