---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - crates/ratemypcb-core/src/dfm.rs
  - crates/ratemypcb-core/src/lib.rs
  - crates/ratemypcb-core/tests/dfm_release.rs
  - crates/ratemypcb-core/tests/fabrication_release.rs
  - crates/ratemypcb-core/tests/schematic_release.rs
  - crates/ratemypcb-cli/src/main.rs
  - crates/ratemypcb-cli/tests/decision_report.rs
  - tests/fixtures/dfm/manifest.json
  - tests/fixtures/dfm/declarations.json
autonomous: true
requirements: [DFM-01, DFM-02, DFM-05, DFM-06]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Exact external Phase 6 receipts 06-01-SUMMARY.md and 06-08-SUMMARY.md record no-go for ODB++ and IPC-2581 this release, FMT-03/FMT-05 complete, FMT-04 N/A, and 8/8 closure; Plan 07-03 is unblocked without private parser/corpus use or support claims."
    - "Before any geometry or construction plan, one bounded local CLI→ReviewOptions→core seam normalizes explicit source/version-bound fabrication profile/project rules into fixed-point ManufacturingConstraint and representable customer order/profile acknowledgements into existing ConstructionEvidence/constraints."
    - "Missing, duplicate, stale, partial, inapplicable, or unrepresented threshold authority stays not_checked; unrepresented order/profile facts emit confirmation gaps only, and anonymous Preset f64 values never qualify Phase 7 production behavior."
    - "Only exact deterministic family/version entries with defined precision >= 0.95, reported recall, green mutations, fixture digests, and reviewed promotion metadata can select Blocking (D-07)."
    - "D-09 ranks incomplete required evidence before qualified blockers before EvidenceOnly attention, with deterministic smallest-unblock ties and no score input."
    - "A non-approve assessment P1 intersects the top core-ranked evidence-ref set; the viewer only renders the already validated action order (D-02)."
  artifacts:
    - path: "tests/fixtures/dfm/declarations.json"
      provides: "Bounded source-linked fabrication-rule and customer order/profile authority exercised through the production input path"
    - path: "crates/ratemypcb-core/src/dfm.rs"
      provides: "Declaration normalization, central family qualification, and deterministic unblock-ranking helpers in the one Phase 7 module"
    - path: "crates/ratemypcb-core/src/lib.rs"
      provides: "Existing FabricationReview convergence and report/assessment validator extensions, not a second model or approval engine"
  key_links:
    - from: "crates/ratemypcb-cli/src/main.rs"
      to: "FabricationReview.constraints/construction"
      via: "one optional local declaration file parsed once through ReviewOptions and normalized to existing fixed-point/provenance contracts"
    - from: "07-03 authority seam"
      to: "07-04 through 07-07"
      via: "direct Plan 07-04 dependency and the existing sequential plan chain"
    - from: "validate_report"
      to: "dfm family qualification"
      via: "reject forged or stale Blocking impacts"
    - from: "validate_assessment"
      to: "core top-unblock evidence refs"
      via: "priority-1 evidence intersection"
---

<objective>
Add the single production authority input seam, then close qualification and smallest-release-unblock policy around the tracer using existing fixed-point/report contracts.

Purpose: Resolve production threshold and order/profile authority before geometry/construction breadth, and implement D-07/D-09 once so later families only add measured observations and corpus rows.
Output: Source-linked fixed-point constraints/representable construction authority, fail-closed family/version impact selection, deterministic top-unblock evidence refs, and P1 validation.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@/Users/mattiafiumara/.paseo/worktrees/3s4r2ob6/phase6-interchange-decision/.planning/phases/06-intelligent-interchange-decision-gate/06-01-SUMMARY.md
@/Users/mattiafiumara/.paseo/worktrees/3s4r2ob6/phase6-interchange-decision/.planning/phases/06-intelligent-interchange-decision-gate/06-08-SUMMARY.md
@crates/ratemypcb-core/src/fabrication.rs
@crates/ratemypcb-core/src/dfm.rs
@crates/ratemypcb-core/src/lib.rs
@crates/ratemypcb-cli/src/main.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize bounded fabrication and order authority into existing contracts</name>
  <precondition>Verified external receipts `06-01-SUMMARY.md` and `06-08-SUMMARY.md` record Phase 6 8/8 and no-go for both ODB++ and IPC-2581 this release; Plan 07-03 is unblocked, but private parser/corpus use and intelligent-format support claims remain prohibited.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-core/tests/fabrication_release.rs, crates/ratemypcb-core/tests/schematic_release.rs, crates/ratemypcb-cli/src/main.rs, crates/ratemypcb-cli/tests/decision_report.rs, tests/fixtures/dfm/declarations.json</files>
  <read_first>External Phase 6 receipts `/Users/mattiafiumara/.paseo/worktrees/3s4r2ob6/phase6-interchange-decision/.planning/phases/06-intelligent-interchange-decision-gate/06-01-SUMMARY.md` and `06-08-SUMMARY.md`; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-core/tests/fabrication_release.rs; crates/ratemypcb-core/tests/schematic_release.rs; crates/ratemypcb-cli/src/main.rs; crates/ratemypcb-cli/tests/decision_report.rs</read_first>
  <behavior>- One optional `--dfm-declarations &lt;local-json&gt;` value reaches `review()` through `ReviewOptions` with schema version, source path/digest, producer/version, structural location, applicability, exact decimal values, and declared units; absent input leaves every dependent family not_checked.
- Explicit fabrication-profile/project rules for minimum drill, clearance, copper edge, annular ring, mask sliver, and paste/mask limits normalize to unique fixed-point `ManufacturingConstraint` records with `ManufacturingProvenance`: use the existing semantic `ConstraintKind` where present, and use `ConstraintKind::Other` only for a closed/versioned set of exact edge/mask/paste rule IDs with explicit applicability. Unknown rule IDs are unrepresented/not_checked. Anonymous `Preset` f64 values and direct test-only constraint injection are not production authority.
- Explicit customer order/profile acknowledgements normalize only into existing representable `ConstructionEvidence` and semantically matching constraints (layer material/thickness/order where represented, total thickness, finish, impedance, material, and special process). Unrepresented drill-span/plating, castellation/edge-plating, or profile facts are retained only as confirmation-gap evidence and cannot produce match/conflict.
- Unknown versions/fields/units, duplicate or conflicting authority, stale/partial records, non-exact/out-of-range values, dangling applicability IDs, or unbounded collections are rejected or marked unqualified before analysis and cannot improve coverage, GateImpact, ranking, or approval.</behavior>
  <action>Move the existing planned declaration seam forward from Plan 07-09 and implement it once. Add one optional `DfmDeclarations` value to `ReviewOptions` and one local CLI flag that parses bounded JSON once; hash the original bytes and preserve source/version/location metadata. At the existing core convergence point, convert exact decimal values with declared units to `Picometres` and merge profile/project rules into `FabricationReview.constraints` without overwriting duplicates/conflicts. Map drill/clearance/annular rules to their existing semantic kinds; map edge/mask/paste limits only to `ConstraintKind::Other` records whose IDs belong to the closed/versioned declaration set and retain exact applicability/provenance. Normalize customer order/profile acknowledgements only into `ConstructionLayer` records or semantically matching constraints; use constraints for scalar total-thickness/finish/order requirements so authority/provenance is not lost, never overwrite design-side construction, and do not encode missing order concepts as `Other`, add model fields, or create a second board/order model. Preserve unrepresented acknowledgements as explicit confirmation-gap inputs only. Geometry/construction analyzers must consume only these normalized canonical facts and never `Preset` f64s or fixture-only injected constraints. Initialize every exhaustive `ReviewOptions` constructor in `lib.rs`, CLI `main.rs`, `fabrication_release.rs`, and `schematic_release.rs` with `Some(validated_declarations)` only on the selected CLI path and `None` elsewhere. Add production-path tests for valid profile/project/order authority, absent authority, duplicates, stale/partial/unbounded values, exact unit conversion, provenance, merge conflicts, and unrepresented-order gaps. No adapter, viewer, schema, approval engine, or new framework is added.</action>
  <verify><automated>cargo test -p ratemypcb-cli --test decision_report --locked dfm_authority_ &amp;&amp; cargo test -p ratemypcb-core --test dfm_release authority_ --locked &amp;&amp; cargo test -p ratemypcb-core --tests --locked --no-run</automated></verify>
  <acceptance_criteria>The real CLI→ReviewOptions→core path produces fixed-point, source-linked existing constraint/construction facts for represented inputs; missing/invalid authority stays not_checked; unrepresented order facts produce confirmation gaps only; anonymous presets and direct fixture constraints cannot qualify a production family; every exhaustive `ReviewOptions` constructor compiles with an explicit optional value.</acceptance_criteria>
  <done>The one bounded production authority seam exists before any threshold- or order-dependent analyzer plan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce family/version qualification before GateImpact</name>
  <precondition>Verified external receipts `06-01-SUMMARY.md` and `06-08-SUMMARY.md` record Phase 6 8/8 and no-go for both ODB++ and IPC-2581 this release; Plan 07-03 is unblocked, but private parser/corpus use and intelligent-format support claims remain prohibited.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json</files>
  <read_first>External Phase 6 receipts `/Users/mattiafiumara/.paseo/worktrees/3s4r2ob6/phase6-interchange-decision/.planning/phases/06-intelligent-interchange-decision-gate/06-01-SUMMARY.md` and `06-08-SUMMARY.md`; .planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-PROMOTION-CHECKPOINT.md; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-core/tests/dfm_release.rs; tests/fixtures/dfm/manifest.json</read_first>
  <behavior>- Missing/duplicate/stale/unknown/version-mismatched promotion metadata downgrades to EvidenceOnly and never creates pass.
- Undefined precision or recall, precision below 0.95, absent positive/hard-negative targets, or any red mutation cannot select Blocking.
- Inference remains EvidenceOnly unless the exact family/version has the separate human Markdown approval required by D-08.</behavior>
  <action>Add one small static family/version policy table in `dfm.rs`, not a configurable registry. It consumes test-reviewed qualification facts and returns only `GateImpact`; analyzers never construct final impact. Extend `validate_report` to recompute and reject forged impact using the existing validator/approval engine per D-01. Deterministic population remains EvidenceOnly unless its manifest contains complete reviewed promotion metadata; do not invent a reviewer. Inference additionally requires an exact approve record in `07-PROMOTION-CHECKPOINT.md`, and removal downgrades. Keep qualification metrics in the project-authored manifest/test evaluator, not runtime plugins.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release qualification_ --locked</automated></verify>
  <acceptance_criteria>Positive eligibility, under-threshold, undefined precision/recall, missing corpus class, mutation failure, duplicate/version mismatch, removed promotion, and forged Blocking mutations all have deterministic expected results; no inference family is Blocking.</acceptance_criteria>
  <done>GateImpact is selected centrally and fail-closed for every family/version.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rank the deterministic smallest release unblock and bind P1</name>
  <precondition>Verified external receipts `06-01-SUMMARY.md` and `06-08-SUMMARY.md` record Phase 6 8/8 and no-go for both ODB++ and IPC-2581 this release; Plan 07-03 is unblocked, but private parser/corpus use and intelligent-format support claims remain prohibited.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs</files>
  <read_first>External Phase 6 receipts `/Users/mattiafiumara/.paseo/worktrees/3s4r2ob6/phase6-interchange-decision/.planning/phases/06-intelligent-interchange-decision-gate/06-01-SUMMARY.md` and `06-08-SUMMARY.md`; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/lib.rs; crates/ratemypcb-core/tests/dfm_release.rs; tests/fixtures/decision-report/blocked-report.json; tests/fixtures/decision-report/blocked-assessment.json</read_first>
  <behavior>- Required evidence with incomplete execution, non-pass result, or invalid freshness is tier 1 in existing `required_coverage` order then check ID.
- Qualified medium-or-higher Blocking findings are tier 2 by severity descending, family/version, canonical location/target, evidence ID; tier 3 is EvidenceOnly attention by family/location.
- Score mutations do not change ranking; P1 on revise/blocked must intersect the nonempty top set; actions remain unique and at most three.</behavior>
  <action>Implement one core helper returning the acceptable top evidence-ref set, not prose or a score. Give a missing required coverage occurrence an existing canonical evidence-bearing coverage record so empty IDs can never satisfy P1. Deduplicate only identical declared corrective-action keys; otherwise preserve occurrences. Call the helper from `validate_assessment` and require priority 1 for non-approve dispositions to intersect the set. Preserve current action prose, three-action cap, unique priorities, evidence finalization, required summary, and `approval_eligible` per D-01/D-09. Do not modify viewer sorting or create recommendations.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release unblock_ --locked &amp;&amp; cargo test -p ratemypcb-core assessment --locked</automated></verify>
  <acceptance_criteria>Tier precedence, all tie-breaks, score independence, dedupe-key boundaries, empty-evidence repair, P1 intersection, missing P1, and three-action cap pass mutation tests; viewer assets remain unchanged.</acceptance_criteria>
  <done>Core supplies one deterministic smallest-unblock set and existing assessment validation enforces P1 against it.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
| --- | --- |
| local declaration bytes → canonical constraints/construction | Malformed, stale, duplicate, or anonymous authority could be elevated into production policy. |
| analyzer output → release gate | An analyzer could forge Blocking or pass. |
| report → human assessment | Assessment action references could bypass the top release unblock. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-26 | Spoofing | profile/project/order authority | high | mitigate | Bound/hash local input, require source/version/applicability, convert exact units, reject duplicates/staleness, and normalize only existing represented facts. |
| T-07-06 | Elevation of Privilege | family GateImpact | high | mitigate | Recompute exact family/version eligibility in `validate_report`. |
| T-07-07 | Tampering | P1 evidence refs | high | mitigate | `validate_assessment` requires intersection with core top set and valid nonempty evidence IDs. |
| T-07-08 | Repudiation | promotion record | medium | mitigate | Require reviewed family/version metadata and inference-specific Markdown approval. |
| T-07-SC | Tampering | package installs | low | accept | No package install or new dependency exists. |
</threat_model>

<requirements_coverage>

- DFM-01: closes the production threshold-authority path before drill/clearance/edge/annular/mask/paste families.
- DFM-02: closes production order/profile authority for existing represented facts and makes every unrepresented fact an explicit confirmation gap.
- DFM-05: central qualification, metrics gates, mutation gates, and promotion downgrade.
- DFM-06: smallest-unblock tiering, deterministic ties, score independence, and P1 enforcement.
</requirements_coverage>

<artifacts_this_phase_produces>

- One bounded CLI→core declaration seam normalized into existing fixed-point constraints/construction.
- One central qualification table and one core top-unblock helper inside the existing plain module.
- Existing report/assessment validator extensions; no second model, approval, or recommendation engine.
</artifacts_this_phase_produces>

<verification>
Focused authority/qualification/ranking tests, exhaustive `ReviewOptions` compile coverage, existing assessment tests, fmt, and `git diff --check` pass.
</verification>

<success_criteria>
No threshold/order-dependent family can run without source-bound canonical authority, no unqualified family can block, missing required evidence always ranks first, and P1 must reference the core-ranked unblock set. Phase 6 no-go introduces no ODB++/IPC-2581 integration, parity, corpus, or support claim.
</success_criteria>

<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-03-SUMMARY.md` when done.</output>
