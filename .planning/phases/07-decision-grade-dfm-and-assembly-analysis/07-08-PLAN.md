---
phase: 07-decision-grade-dfm-and-assembly-analysis
plan: 08
type: execute
wave: 8
depends_on: [07-07]
files_modified: [crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/fabrication/native.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json]
autonomous: true
requirements: [DFM-03, DFM-05]
estimate: {tokens: 27000, raw_tokens: 27000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "Production native normalization emits policy-free AssemblyPlacement and typed courtyard observations before side/rotation, paste, native courtyard, and footprint families consume source-linked facts (D-01/D-02/D-07)."
    - "Unknown conventions and layer presence are not_checked; distributor packaging never establishes physical package compatibility (D-04/D-05)."
  artifacts:
    - {path: "tests/fixtures/dfm/assembly-targets.json", provides: "Assembly deterministic corpus"}
    - {path: "crates/ratemypcb-core/src/dfm.rs", provides: "Deterministic assembly families"}
  key_links:
    - {from: "assembly families", to: "normalized placement/BOM/occurrences/FabricationReview/native DRC", via: "exact identity and source provenance"}
---

<objective>
Complete deterministic assembly correlation beyond the population tracer.

Purpose: Deliver side/rotation, actual paste availability, native courtyard evidence, and bounded footprint consistency without physical-package guesses.
Output: Four deterministic family implementations and qualification fixtures.
</objective>

<execution_context>
@~/.codex/gsd-core/workflows/execute-plan.md
@~/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-CONTEXT.md
@.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-RESEARCH.md
@crates/ratemypcb-core/src/dfm.rs
@crates/ratemypcb-core/src/schematic.rs
@crates/ratemypcb-core/src/fabrication/native.rs
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Emit policy-free native assembly placement and courtyard facts</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/fabrication.rs, crates/ratemypcb-core/src/fabrication/native.rs, crates/ratemypcb-core/tests/dfm_release.rs, crates/ratemypcb-core/tests/fabrication_release.rs, schemas/report-2.0.json, tests/fixtures/dfm/assembly-targets.json</files>
  <read_first>The canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/fabrication/native.rs; crates/ratemypcb-core/tests/fabrication_release.rs; schemas/report-2.0.json; tests/fixtures/kicad/mismatch/root-positions.csv</read_first>
  <behavior>- Native KiCad component objects normalize reference, exact X/Y, declared unit/origin, top/bottom side, rotation in microdegrees, fitted state, revision, artifact digest, and structural source location into `AssemblyPlacement`.
- `CapabilityId::Assembly` is Complete only when the placement collection and conventions are complete, unique, source-linked, and free of affected omissions/conflicts; partial or unknown facts preserve their non-complete state.
- Completed supported native DRC output normalizes typed courtyard overlap/malformed/missing observations with tool/version/location and active/excluded/unknown exclusion state; failed or absent execution never becomes clean evidence.</behavior>
  <action>Extend only the existing canonical `AssemblyEvidence` and native normalizer: populate `placements` from authoritative native component objects and add the smallest typed native-courtyard observation record needed by DFM-03. Keep parsing/normalization policy-free; do not assign `GateImpact`, family qualification, or pass/blocking in `crates/ratemypcb-core/src/fabrication/native.rs`. Validate exact decimal conversion, side/rotation convention, unique occurrence identity, source provenance, bounded counts, and capability state before emitting `CapabilityId::Assembly`. Update the exhaustive `AssemblyEvidence` fixture in `fabrication_release.rs` and regenerate `schemas/report-2.0.json` from authoritative `report_schema()` for every additive serialized field. Add a real native fixture control plus missing/duplicate/invalid/convention/provenance/native-DRC-failure mutations.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release native_assembly_ --locked &amp;&amp; cargo test -p ratemypcb-core --test fabrication_release native_ --locked &amp;&amp; cargo test -p ratemypcb-core --test fabrication_release model_generated_schema_is_semantically_and_byte_equal --locked</automated></verify>
  <acceptance_criteria>Production native review emits complete source-linked assembly facts for the control and honest non-complete states for every mutation; fixtures cannot bypass the production normalizer; the exhaustive fabrication fixture compiles; generated and checked-in report schemas are byte-equal; no adapter policy or viewer change exists.</acceptance_criteria>
  <done>Canonical production assembly facts exist before deterministic assembly analyzers consume them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compare side/rotation and check actual fitted-pad paste/native courtyard evidence</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/src/fabrication.rs; crates/ratemypcb-core/src/fabrication/native.rs; crates/ratemypcb-core/tests/dfm_release.rs</read_first>
  <behavior>- `assembly.side-rotation.v1` compares exact side and microdegree rotation only after unit/origin/top-bottom/mirroring/direction conventions are explicit; unknown conventions are not_checked.
- `assembly.paste-availability.v1` requires fitted state, placed side, pad identity, and actual paste geometry.
- `assembly.courtyard-native.v1` preserves completed normalized native tool/version/exclusion/location semantics rather than recreating KiCad DRC; layer presence or incomplete native execution is not clean evidence.</behavior>
  <action>Consume the production facts from Task 1. Compare declared placement rows to canonical native placements with exact convention normalization, reuse the DFM paste geometry result per D-01, and classify typed completed native courtyard observations only. Never infer conventions from headers, rebuild KiCad courtyard semantics, or infer paste from layer IDs per D-05. Add equivalent-angle, unknown-convention, DNP/windowpane/deliberate-omission, native-failure/exclusion hard negatives and mutation metrics.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release side_rotation_ --locked &amp;&amp; cargo test -p ratemypcb-core --test dfm_release assembly_paste_ --locked &amp;&amp; cargo test -p ratemypcb-core --test dfm_release courtyard_native_ --locked</automated></verify>
  <acceptance_criteria>Top/bottom/mirroring/rotation, actual fitted-pad, layer-presence-only, native completed/failure/unknown exclusion, provenance and promotion mutations pass; fixtures exercise the real production facts and unknown input never passes/blocks.</acceptance_criteria>
  <done>Paste and courtyard outputs reuse authoritative geometry/native evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Qualify footprint source-string parity without package compatibility claims</name>
  <precondition>Phase 6 is complete and its canonical decision artifact, as named by the completed Phase 6 summary/verification, contains exactly one adopt-one, adopt-both, or no-go result; halt before editing if the artifact is missing, ambiguous, or malformed.</precondition>
  <files>crates/ratemypcb-core/src/dfm.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/dfm_release.rs, tests/fixtures/dfm/manifest.json, tests/fixtures/dfm/assembly-targets.json</files>
  <read_first>the canonical Phase 6 decision artifact named by the completed Phase 6 summary/verification; crates/ratemypcb-core/src/schematic.rs; crates/ratemypcb-core/src/dfm.rs; crates/ratemypcb-core/tests/schematic_release.rs; tests/fixtures/dfm/manifest.json</read_first>
  <behavior>- `assembly.footprint-string-parity.v1` freezes exact/full-string and explicitly adjudicated library-suffix behavior with dual provenance.
- Ambiguous reference fallback is not_checked.
- Distributor tape/reel/tray and footprint-name similarity cannot produce physical compatibility.</behavior>
  <action>Consume only the named footprint mismatch subset from existing typed `SchematicReview.mismatches`; do not rerun footprint comparison, exact/suffix matching, occurrence identity, or unique-reference fallback in `dfm.rs`, and do not globally promote existing reconciliation. Freeze the existing exact/suffix behavior in corpus targets, preserve the typed mismatch join/confidence/location, and reject package compatibility claims per D-05. Add only family qualification/evidence mapping, positive/hard-negative/mutation metrics, and central impact selection.</action>
  <verify><automated>cargo test -p ratemypcb-core --test dfm_release footprint_string_ --locked &amp;&amp; cargo test -p ratemypcb-core --test schematic_release reconciliation_ --locked</automated></verify>
  <acceptance_criteria>Exact/suffix/mismatch/ambiguous/duplicate cases pass; packaging/name heuristic mutations remain not_checked; original schematic evidence remains EvidenceOnly unless this exact family qualifies.</acceptance_criteria>
  <done>Footprint consistency is bounded to source-string parity and never claims physical compatibility.</done>
</task>
</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|---|---|
| placement/native/BOM facts → assembly conclusions | Weak conventions or labels could be elevated into deterministic claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-07-17 | Spoofing | side/paste/package semantics | high | mitigate | Explicit convention, actual geometry and exact string authority only. |
| T-07-18 | Tampering | native courtyard status | medium | mitigate | Preserve completed tool/version/exclusion states; failures are not_checked. |
| T-07-SC | Tampering | installs | low | accept | No new dependency. |
</threat_model>
<requirements_coverage>- DFM-03: side/rotation, paste, courtyard, and bounded footprint consistency.

- DFM-05: qualification evidence per family.</requirements_coverage>
<artifacts_this_phase_produces>- Four deterministic assembly families and corpus.</artifacts_this_phase_produces>
<verification>Focused assembly tests, schematic regressions, fmt and clippy pass.</verification>
<success_criteria>Deterministic assembly findings are source-linked and never overclaim convention, paste, native, or package authority.</success_criteria>
<output>Create `.planning/phases/07-decision-grade-dfm-and-assembly-analysis/07-08-SUMMARY.md` when done.</output>
