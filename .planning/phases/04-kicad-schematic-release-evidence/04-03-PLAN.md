---
phase: 04-kicad-schematic-release-evidence
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - crates/ratemypcb-core/src/schematic.rs
  - crates/ratemypcb-core/src/lib.rs
  - crates/ratemypcb-core/tests/schematic_release.rs
  - crates/ratemypcb-cli/src/main.rs
  - crates/ratemypcb-cli/assets/local-viewer.js
  - crates/ratemypcb-cli/tests/decision_report.rs
  - schemas/report-2.0.json
  - tests/report-contract.test.mjs
  - tests/report-ux.test.mjs
  - skills/review-pcb-dfm/SKILL.md
  - skills/review-pcb-dfm/references/report-contract.md
  - README.md
requirements: [SCH-01, SCH-02, SCH-03, SCH-04, SCH-05, SCH-06, SCH-07, SCH-08]
autonomous: true
must_haves:
  truths:
    - CLI, report/schema, viewer, skill, and README expose one capability-specific KiCad 8/9/10 contract.
    - `--schematic` resolves only automatic-root ambiguity and cannot bypass project/path bounds.
    - ERC, parity, hierarchy, fact, and reconciliation evidence remains provenance-linked through report digest, assessment validation, and self-contained HTML.
    - Adversarial tool, hierarchy, exclusion, mismatch, capability, and identity mutations cannot create a pass or approval.
    - No schematic family is required or blocking until a separately approved corpus checkpoint promotes it.
  artifacts:
    - path: schemas/report-2.0.json
      provides: Generated additive schematic/ERC/parity/capability/gate contract.
    - path: crates/ratemypcb-cli/tests/decision_report.rs
      provides: CLI report-to-digest-to-assessment-to-HTML schematic tracer and exit checks.
    - path: tests/report-contract.test.mjs
      provides: Schema/evidence/gate/capability adversarial regressions.
    - path: skills/review-pcb-dfm/references/report-contract.md
      provides: User-facing authority, version, exclusion, pairing, and non-blocking semantics.
  key_links:
    - from: crates/ratemypcb-cli/src/main.rs
      to: crates/ratemypcb-core/src/lib.rs
      via: Bounded schematic selector and unchanged review error/exit mapping.
    - from: crates/ratemypcb-cli/assets/local-viewer.js
      to: schemas/report-2.0.json
      via: Viewer consumes normalized core fields and evidence IDs without deriving policy.
    - from: skills/review-pcb-dfm/SKILL.md
      to: skills/review-pcb-dfm/references/report-contract.md
      via: Same native-first capability and fail-closed assessment workflow.
---

<objective>Finish the Phase 4 vertical slice through CLI, report/schema, viewer, skill/docs, and adversarial/full-suite gates without promoting unadjudicated schematic evidence.</objective>

<ownership>
This plan owns the explicit CLI selector and capability output, final additive report/schema contract, viewer consumption, skill/README wording, cross-layer tracer, adversarial mutations, and full phase gate. Core edits are limited to integration defects exposed by these gates; 04-01 native semantics and 04-02 hierarchy/reconciliation algorithms are not redesigned here.
</ownership>

<threat_boundaries>

- T4-01/T4-02 CLI: selector is a normalized project-relative match; invalid/ambiguous input exits 2, required native failure exits 3, auto not-run still returns a report, and ZIP native checks remain disabled.
- T4-05 contract: exact version and artifact/pair digests survive serialization; evidence IDs remain stable across order/prose and distinct across UUID paths; evidence-only gate state is validated rather than trusted.
- T4-06 presentation: viewer inserts producer strings, paths, descriptions, and comments with `textContent`/existing escaped payload; it never recomputes exclusion, reconciliation, severity, or approval.
- T4-07 alignment: docs distinguish local KiCad 10 execution from documentation-attested 8/9 fixtures and never claim native Altium/generic-netlist parity.
</threat_boundaries>

<tasks>

<task type="auto" tdd="true">
  <name>04-03-01: Integrate CLI, final schema, viewer, and skill capability contract</name>
  <files>crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-cli/src/main.rs, crates/ratemypcb-cli/assets/local-viewer.js, crates/ratemypcb-cli/tests/decision_report.rs, schemas/report-2.0.json, tests/report-contract.test.mjs, tests/report-ux.test.mjs, skills/review-pcb-dfm/SKILL.md, skills/review-pcb-dfm/references/report-contract.md, README.md</files>
  <action>Add optional `--schematic PATH` to `ReviewOptions`, parser/help, and review loading solely as an override for ambiguous automatic root selection; normalize it like `--board` and reject non-project/external/duplicate matches. Replace broad doctor wording with structured detected version/major/support plus explicit PCB DRC, schematic ERC, coherent-project parity, supported majors `[8,9,10]`, and ZIP/Altium/netlist limitations. Finalize additive report 2.0 fields for selected schematic/project identity, hierarchy capability, native ERC, parity source pair, normalized markers/exclusions, fact/reconciliation summaries, capability limitations, and per-finding gate impact; regenerate the checked schema. Give every visible marker/mismatch/capability record resolvable provenance and canonical UUID-path/item locations. Extend the existing generic viewer/category/evidence surfaces, not a second analysis panel: display sheet/channel/source-pair/version/exclusion state and gate impact using core values, existing progressive evidence loading, and safe text insertion. Update skill/report-contract/README commands and reasoning rules for complete project input, `--schematic`, exit 0/5 native semantics, exclusions unknown, coherent parity, exact-first joins, evidence-only families, and bounded Altium/netlist/ZIP claims. Do not add heuristics or promote schematic checks into required evidence.</action>
  <verify><automated>cargo test -p ratemypcb-cli --test decision_report --locked schematic_ &amp;&amp; node --test tests/report-contract.test.mjs tests/report-ux.test.mjs</automated></verify>
  <done>An ambiguous project can be resolved explicitly; doctor/help are capability-specific; report/schema/viewer/skill/docs agree; native not-run and marker provenance survive exact digest and HTML rendering without client policy.</done>
</task>

<task type="auto" tdd="true">
  <name>04-03-02: Close adversarial, identity, gate, and full-phase regressions</name>
  <files>crates/ratemypcb-core/src/schematic.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/schematic_release.rs, crates/ratemypcb-cli/tests/decision_report.rs, tests/report-contract.test.mjs, tests/report-ux.test.mjs, schemas/report-2.0.json, skills/review-pcb-dfm/references/report-contract.md</files>
  <action>Complete one adjudicated positive/hard-negative/mutation matrix for every deterministic schematic family and assert all remain `evidence_only`; reject any mutation that inserts a schematic check into `requiredEvidence`, changes gate impact to blocking without an approved registry entry, or lets excluded-only markers affect findings/approval. Cover unavailable/timeout/7/11/nonzero/missing/oversized/malformed/truncated/version mismatch, exit 5 completion, exclusion true/false/unknown, distinct ERC/DRC/unconnected/parity channels, missing child/unresolved variable/ambiguous root/broken path/duplicate/cycle, reused-sheet stable/distinct IDs, every SCH-06 mismatch, weak-reference fallback, stale revision, ZIP not-run, `.SchDoc`, recognized/unknown netlists, and report producer/pair digests. Add CLI assertions for selector errors (2), required native failures (3), auto report production, exact report-byte digest, assessment evidence references, self-contained rendering, safe marker text, and honest doctor output. Run generated-schema equality and full Rust/Node/format/diff gates. Fix only integration defects within listed files; do not loosen bounds or claims to make a fixture pass.</action>
  <verify><automated>cargo fmt --all -- --check &amp;&amp; cargo test --all --locked &amp;&amp; node --test tests/board-view.test.mjs tests/report-contract.test.mjs tests/report-ux.test.mjs &amp;&amp; git diff --check</automated></verify>
  <done>All Phase 4 fixtures and mutations pass the declared expectations, schema equality is green, no unsupported state becomes pass/approval, every evidence link resolves, and no new family is blocking or required.</done>
</task>

</tasks>

<verification>
In addition to the task command, inspect `ratemypcb doctor --json` and one sanitized Phase 4 report manually for exact capability wording, producer versions, source-pair digests, unknown exclusion state, evidence-only gate state, and absence of absolute checkout paths. Record corpus results and the still-closed promotion checkpoint in `04-VERIFICATION.md` during execution.
</verification>

<success_criteria>

- A coherent hierarchical KiCad project produces native ERC/parity/export/reconciliation evidence with exact source/version/provenance and visible failure/exclusion states.
- Reused sheets, multi-unit symbols, power intent, fitted/DNP, and occurrence paths are not reference-conflated.
- Every seeded supported mismatch is detected and evidence-linked.
- Tool/context/version/ZIP/Altium/netlist limitations remain explicit not-run/not-checked states.
- CLI, schema, viewer, skill, and docs agree, all phase gates pass, and the schematic promotion checkpoint remains closed.
</success_criteria>
