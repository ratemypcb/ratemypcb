---
phase: 04-kicad-schematic-release-evidence
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - crates/ratemypcb-core/src/schematic.rs
  - crates/ratemypcb-core/src/lib.rs
  - crates/ratemypcb-core/tests/schematic_release.rs
  - schemas/report-2.0.json
  - tests/fixtures/kicad/hierarchy/
  - tests/fixtures/kicad/mismatch/
  - tests/fixtures/bounded-eda/
requirements: [SCH-01, SCH-02, SCH-04, SCH-05, SCH-06, SCH-07, SCH-08]
autonomous: true
must_haves:
  truths:
    - One coherent KiCad project/root and bounded child graph drive ERC, exports, parity, facts, reconciliation, digests, and provenance.
    - Reused-sheet occurrences are keyed by project/root plus UUID path and item UUID, never by reference or child filename alone.
    - Native exports are authoritative and every source fallback fact declares its producer/evidence class.
    - Exact-first staged joins detect every required schematic, board, BOM, placement, connectivity, and revision mismatch.
    - Altium, unknown netlists, generic exports, and ZIP inputs cannot inflate native ERC, hierarchy, parity, DNP, or source-aware capabilities.
    - All new schematic families remain explicitly evidence-only and non-blocking.
  artifacts:
    - path: crates/ratemypcb-core/src/schematic.rs
      provides: Bounded hierarchy/fact model, native exports, and exact-first reconciliation.
    - path: crates/ratemypcb-core/tests/schematic_release.rs
      provides: Released-major, hierarchy, mismatch, and capability integration coverage.
    - path: tests/fixtures/kicad/hierarchy/manifest.json
      provides: Adversarial hierarchy case inventory and expected typed outcomes.
    - path: tests/fixtures/kicad/mismatch/mutations.json
      provides: One deterministic mutation per SCH-06 mismatch class plus clean control.
    - path: tests/fixtures/bounded-eda/manifest.json
      provides: Bounded `.SchDoc` and generic-netlist capability expectations.
  key_links:
    - from: crates/ratemypcb-core/src/lib.rs
      to: crates/ratemypcb-core/src/schematic.rs
      via: `load_path()`/`review()` pass one selected project context into inventory, native checks, exports, and reconciliation.
    - from: crates/ratemypcb-core/src/schematic.rs
      to: crates/ratemypcb-core/tests/schematic_release.rs
      via: Public `review()` output exposes normalized schematic evidence and stable occurrence-linked mismatches.
---

<objective>Add bounded project/hierarchy inventory, source/export facts, exact-first cross-artifact reconciliation, and honest non-KiCad capability states on top of the native runner.</objective>

<ownership>
This plan owns core classification/loading, automatic root selection, hierarchy/source parsing, native export normalization, board/BOM/placement fact enrichment, reconciliation check families, capability states, core report wiring, and their schema slice. It calls the 04-01 runner but does not change its process/exit semantics. It does not add the CLI `--schematic` surface, doctor/help wording, viewer rendering, skill prose, or final cross-layer adversarial gate; those belong to 04-03.
</ownership>

<threat_boundaries>

- T4-02 paths: child references are normalized relative paths confined to the project root; missing/external/variable/cycle/duplicate cases are explicit; ZIP is inventory-only for native execution.
- T4-03 parser: bounded bytes, UTF-8, tokens, nesting, string length, child/occurrence/component/pin/net counts; over-limit or unsupported syntax becomes typed not-checked, never partial success.
- T4-04 identity: project + root digest + sheet UUID path + item UUID is primary; board footprint UUID/path is secondary; reference fallback is unique-only and labeled weak.
- T4-05 gate/provenance: each fact/mismatch has exact producer, source digest/path, structured occurrence location, confidence, and `evidence_only` gate impact.
- T4-06 claims: `.SchDoc` inventory and recognized generic export fields cannot create native/source-aware capabilities.
</threat_boundaries>

<tasks>

<task type="auto" tdd="true">
  <name>04-02-01: Inventory coherent roots and reconstruct bounded occurrence identity</name>
  <files>crates/ratemypcb-core/src/schematic.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/schematic_release.rs, schemas/report-2.0.json, tests/fixtures/kicad/hierarchy/{manifest.json,missing-child/root.kicad_sch,unresolved-variable/root.kicad_sch,ambiguous-roots/alpha.kicad_sch,ambiguous-roots/beta.kicad_sch,broken-instance-path/root.kicad_sch,broken-instance-path/child.kicad_sch,reused-child/root.kicad_sch,reused-child/child.kicad_sch,cycle/root.kicad_sch,cycle/child.kicad_sch}</files>
  <action>Extend `classify`, `collect_dir`, ZIP inventory, `Loaded`, and review setup for standalone `.kicad_sch`, project `.kicad_sch`, `.kicad_pro`, `.kicad_prl` only where needed for context, `.kicad_sym`, `sym-lib-table`, `.SchDoc`, and bounded `.net`/XML candidates. A standalone schematic may support bounded ERC/source facts but never parity without a coherent board/project pair. Retain schematic source text only within explicit per-file/aggregate bounds. Add automatic root selection beside `select_board`: use selected board/project basename and project instances first, build the child graph, remove child-only candidates, and return typed ambiguity when more than one coherent root remains. Implement a bounded stdlib schematic tokenizer/parser for root UUID, sheets/child filenames, variables, project instances, symbol/sheet instance UUID paths, item/symbol UUID, unit/reference, and source path. Key occurrences by project identity + root digest + sheet UUID path + item UUID. Preserve two occurrences of a reused child. Emit explicit capability/evidence states for missing child, unresolved variable, broken/duplicate instance path, root ambiguity, recursion/cycle, unsupported syntax, and limits. Inventory ZIP hierarchy but keep native execution not-run.</action>
  <verify><automated>cargo test -p ratemypcb-core --test schematic_release --locked hierarchy_</automated></verify>
  <done>Directories and ZIPs inventory schematic context safely; one coherent root is selected or ambiguity is explicit; every adversarial hierarchy fixture produces its typed state; reused child occurrences remain distinct.</done>
</task>

<task type="auto" tdd="true">
  <name>04-02-02: Normalize explicit facts and reconcile schematic, PCB, BOM, placement, and connectivity</name>
  <files>crates/ratemypcb-core/src/schematic.rs, crates/ratemypcb-core/src/lib.rs, crates/ratemypcb-core/tests/schematic_release.rs, schemas/report-2.0.json, tests/fixtures/kicad/mismatch/{manifest.json,mutations.json}, tests/fixtures/bounded-eda/{manifest.json,minimal.SchDoc,components-nets.net,components-nets.xml,unknown.net}</files>
  <action>Use 04-01's fixed runner to execute native BOM, `kicadsexpr` netlist, and PCB position exports only for directly accessible coherent projects; never stage ZIP input. Normalize exported occurrence/component/pin/net/population fields as authority. Parse source only for absent explicit facts: occurrence/item/symbol UUID and unit, fields/value/footprint, `in_bom`, `on_board`, DNP/fitted state, pin electrical types, power symbols/flags, and needed connectivity. Extend board facts with footprint UUID/path/reference/value/library footprint, fitted/DNP, pads, nets, and revision/digest; retain BOM rows and placement rows rather than reducing them to sets. Join in strict order: occurrence UUID path+item UUID, board footprint path/UUID, then unique normalized reference with weaker confidence. Compare reference/UUID, value, footprint, fitted/DNP/BOM/board state, pin-pad, net, grouped quantity, placement population, and board/root/BOM/placement revision/digests. Give each mismatch a stable check ID and occurrence/source paths in canonical evidence location. Add a report-authoritative gate-impact field/default so existing findings stay blocking while every new schematic family is `evidence_only`; do not add schematic IDs to `required_evidence_summary`. For `.SchDoc`, emit inventory-only unsupported capabilities. Parse only explicit component/net/pin/sheet-path fields from recognized KiCad s-expression/XML export roots; unknown syntax is unsupported/not-checked and never implies native ERC, hierarchy, DNP, revision, or parity.</action>
  <verify><automated>cargo test -p ratemypcb-core --test schematic_release --locked reconciliation_ &amp;&amp; cargo test -p ratemypcb-core --test schematic_release --locked bounded_eda_</automated></verify>
  <done>The clean released-major controls reconcile; every required mutation produces a source-occurrence-linked mismatch; native/export/source provenance is fact-specific; weak joins are labeled; all new families are visibly non-blocking; Altium/netlist/ZIP states remain bounded.</done>
</task>

</tasks>

<verification>
Run `cargo fmt --all -- --check &amp;&amp; cargo test --all --locked &amp;&amp; git diff --check` after the wave. Confirm generated and checked-in report schema JSON are semantically equal and that `requiredEvidence` contains no new schematic check family.
</verification>

<success_criteria>

- SCH-01/02 hierarchy inventory is bounded, coherent, and occurrence-path based.
- Native exports and explicit source facts satisfy SCH-05 without recreating ERC semantics.
- Clean controls pass reconciliation and all SCH-06 mutation classes are detected with stable source locations.
- Native parity runs only after coherent board/root/project pairing and records both source digests/revision.
- `.SchDoc`, generic netlists, unknown netlists, and ZIPs cannot make unsupported checks look completed.
- Schematic evidence can carry severity/attention but cannot affect approval in this phase.
</success_criteria>
