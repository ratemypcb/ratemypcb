---
phase: 04-kicad-schematic-release-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/ratemypcb-core/src/schematic.rs
  - crates/ratemypcb-core/src/lib.rs
  - schemas/report-2.0.json
  - tests/fixtures/kicad/supported/8/hierarchical/
  - tests/fixtures/kicad/supported/9/hierarchical/
  - tests/fixtures/kicad/supported/10/hierarchical/
  - tests/fixtures/kicad/native-failures/
requirements: [SCH-03, SCH-04, SCH-07]
autonomous: true
must_haves:
  truths:
    - Only explicit KiCad majors 8, 9, and 10 execute; exit 0 and 5 both parse as completed analysis.
    - ERC, ordinary DRC, unconnected, and schematic-parity markers stay distinct, and excluded or unknown-exclusion markers are never discarded.
    - Auto tool failures become provenance-bearing not-run evidence while required-mode failures remain execution errors.
    - Native execution is bounded, shell-free, non-mutating, and records the exact executable/report version and artifact digests.
  artifacts:
    - path: crates/ratemypcb-core/src/schematic.rs
      provides: Explicit released-major adapters, bounded runner, and normalized ERC/DRC records.
    - path: tests/fixtures/kicad/supported/8/hierarchical/manifest.json
      provides: KiCad 8 command/schema provenance without a false local-execution claim.
    - path: tests/fixtures/kicad/supported/9/hierarchical/manifest.json
      provides: KiCad 9 command/schema provenance and exclusion limitation.
    - path: tests/fixtures/kicad/supported/10/hierarchical/manifest.json
      provides: Sanitized locally executable KiCad 10 corpus provenance.
    - path: tests/fixtures/kicad/native-failures/manifest.json
      provides: Unsupported, timeout, nonzero, malformed, truncated, and exclusion cases.
  key_links:
    - from: crates/ratemypcb-core/src/lib.rs
      to: crates/ratemypcb-core/src/schematic.rs
      via: Existing PCB DRC/profile paths call one fixed-operation native runner.
    - from: crates/ratemypcb-core/src/schematic.rs
      to: tests/fixtures/kicad/supported/
      via: Per-major parser/command contract tests.
---

<objective>Establish one bounded native KiCad 8/9/10 execution and marker-normalization seam, then migrate existing PCB DRC onto it without changing project selection or adding hierarchy reconciliation yet.</objective>

<ownership>
This plan owns the native process primitive, explicit major dispatch, command vectors, output normalization, existing PCB DRC/profile migration, native producer provenance, and the released-major/native-failure fixture base. It does not select schematic roots, parse source hierarchy, run exports for reconciliation, add `--schematic`, or document user-facing schematic support; those belong to 04-02/04-03. The schema edit in this wave is limited to the normalized native marker/exclusion/version shape needed to keep generated-schema equality green; 04-03 owns final schematic aggregate fields.
</ownership>

<threat_boundaries>

- T4-01 process: fixed argv only; no shell, `--save-board`, or shared 8/9/10 `--refill-zones`; bounded output/stderr; timeout kill+wait; unique fresh outputs and cleanup.
- T4-03 parser: reject missing, oversized, malformed, truncated, wrong-kind, over-limit, or report/executable-major-mismatched JSON instead of truncating to success.
- T4-05 evidence: preserve all channels and tri-state exclusions; count active findings separately; use exact native version and source/pair digest in provenance.
- T4-07 fixtures: project-authored/sanitized only; manifests identify command, official schema branch, producer version, digest, sanitization, and local-execution status.
</threat_boundaries>

<tasks>

<task type="auto" tdd="true">
  <name>04-01-01: Create the released-major corpus and normalized native adapters</name>
  <files>crates/ratemypcb-core/src/schematic.rs; tests/fixtures/kicad/supported/8/hierarchical/{root.kicad_pro,root.kicad_sch,child.kicad_sch,root.kicad_pcb,bom.csv,positions.csv,netlist.net,version.txt,erc.json,drc.json,manifest.json}; tests/fixtures/kicad/supported/9/hierarchical/{root.kicad_pro,root.kicad_sch,child.kicad_sch,root.kicad_pcb,bom.csv,positions.csv,netlist.net,version.txt,erc.json,drc.json,manifest.json}; tests/fixtures/kicad/supported/10/hierarchical/{root.kicad_pro,root.kicad_sch,child.kicad_sch,root.kicad_pcb,bom.csv,positions.csv,netlist.net,version.txt,erc.json,drc.json,manifest.json}; tests/fixtures/kicad/native-failures/{manifest.json,versions.json,erc-exclusions-9.json,malformed.json,truncated.json}</files>
  <action>Create one project-authored hierarchical design represented for majors 8/9/10 with reused child sheet, multi-unit symbol, power flag, fitted and DNP states, BOM, placement, native netlist, ERC, DRC/parity, version, and command/digest manifests. KiCad 10 data may be captured with local 10.0.5 and sanitized; 8/9 manifests must say documentation/schema-attested unless those binaries actually run. Implement explicit `KiCadMajor::{V8,V9,V10}` parsing and fixed command builders for common ERC/DRC semantics. Normalize ERC sheet path/UUID path and DRC ordinary/unconnected/parity channels into bounded records containing raw producer type/severity/description/items, optional exclusion, comment, included severities, ignored checks, source/date/version, and deterministic structural marker location. Tolerate documented optional fields; reject wrong root shapes or over-limit arrays rather than silently dropping records. Do not infer absent `excluded` as false.</action>
  <verify><automated>cargo test -p ratemypcb-core --locked schematic::tests::native_</automated></verify>
  <done>Fixtures disclose their provenance honestly; majors 8/9/10 parse through explicit adapters; channels, sheet occurrence paths, and exclusion unknowns survive normalization; 7/11/unparseable versions are unsupported.</done>
</task>

<task type="auto" tdd="true">
  <name>04-01-02: Migrate PCB DRC and failure/provenance handling to the bounded runner</name>
  <files>crates/ratemypcb-core/src/schematic.rs, crates/ratemypcb-core/src/lib.rs, schemas/report-2.0.json</files>
  <action>Factor the existing version/timeout/process path into one small fixed-operation runner used by ordinary PCB DRC, optional schematic-parity invocation, and staged profile DRC. Accept only exit 0/5 with a fresh valid bounded report. Map unavailable executable, unsupported major, spawn, timeout, non-0/5, missing/oversized/malformed/truncated output, and report-version mismatch to `not_run` in auto mode while preserving a bounded cause; required mode returns `Error::Native`. Omit mutation flags from the common command. Preserve all normalized markers in the report, derive finding/blocking counts only from non-excluded active markers without treating unknown exclusion as false, and keep parity out of generic DRC summaries. Pass exact native version and exact board/composite-pair digest plus marker structural location into the existing SHA-256 evidence/provenance finalization. Update only the native marker/schema slice and checked-in schema equality; do not add root inventory or final schematic report aggregates.</action>
  <verify><automated>cargo test -p ratemypcb-core --locked native_ &amp;&amp; cargo test -p ratemypcb-core --locked schema_is_versioned</automated></verify>
  <done>Existing PCB/profile checks use one released-major runner, auto failures are not-run, required failures error, exit 5 remains completed, exclusions are retained, parity is distinct, and native provenance no longer says `unknown` when a version was observed.</done>
</task>

</tasks>

<verification>
Run `cargo fmt --all -- --check &amp;&amp; cargo test --all --locked &amp;&amp; git diff --check` after the wave. Inspect all four fixture manifests and confirm no 8/9 local-execution claim was introduced without actual pinned execution.
</verification>

<success_criteria>

- Explicit released-major and exit semantics replace `major &gt;= 8` and every-nonzero failure behavior.
- Excluded markers remain serialized and absence of exclusion remains unknown.
- Ordinary DRC, unconnected, parity, and ERC sheet markers are distinct normalized channels.
- Native auto failures cannot become a completed/pass state; required mode still reaches CLI exit 3 through existing error mapping.
- Existing DRC/profile behavior compiles and full schema/test gates remain green.
</success_criteria>
