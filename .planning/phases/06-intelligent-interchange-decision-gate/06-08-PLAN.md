---
phase: 06-intelligent-interchange-decision-gate
plan: 08
type: execute
wave: 8
depends_on: [06-01, 06-07]
files_modified:
  - .planning/phases/06-intelligent-interchange-decision-gate/06-08-PLAN.md
  - .planning/phases/06-intelligent-interchange-decision-gate/06-08-SUMMARY.md
  - .planning/phases/06-intelligent-interchange-decision-gate/06-VALIDATION.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
requirements: [FMT-05]
autonomous: true
must_haves:
  truths:
    - "The exact FMT-03 no-go decision applies to ODB++ and IPC-2581 for this release only; neither format is adopted, integrated, or supported."
    - "Existing CLI help and doctor JSON identify ODB++ and IPC-2581 as unsupported, while canonical capability prerequisites keep unavailable analysis not checked."
    - "Native KiCad plus Gerber/X2+Excellon remains the strongest supported path and approval behavior is unchanged."
    - "FMT-04 is not applicable under no-go; no adapter, dependency, corpus, classifier, schema, or product implementation is added."
    - "PRIVATE parser SHA a4216f6909754155555e9290c2ec84e0eb16d267 remains quarantined research and grants no production-support claim."
  artifacts:
    - path: crates/ratemypcb-cli/src/main.rs
      provides: Existing no-go product surface reporting both intelligent formats unsupported and retaining Gerber/X2/XNC support.
    - path: crates/ratemypcb-cli/tests/decision_report.rs
      provides: Existing focused executable regression for unsupported formats and unchanged report/CLI behavior.
    - path: .planning/phases/06-intelligent-interchange-decision-gate/06-08-SUMMARY.md
      provides: Verification-only FMT-05 receipt and explicit zero-product-diff result.
  prohibitions:
    - statement: No ODB++/IPC-2581 adapter, dependency, format classifier, corpus bytes, schema/specification, support claim, approval effect, public commit/push, Phase 7 mutation, or custom decision artifact.
      status: active
      verification: explicit
---

<objective>
Close the selected no-go product path by proving the existing unsupported/not-checked CLI and capability behavior already satisfies FMT-05, without inventing product work.
</objective>

<non_goals>

- No intelligent-format parser or product integration.
- No change to native KiCad, Gerber/X2+Excellon, approval, report schema, or viewer behavior.
- No effect from a later ODB++ written response unless a future reopening is explicitly authorized.

</non_goals>

<context>
@.planning/phases/06-intelligent-interchange-decision-gate/06-01-PLAN.md
@.planning/phases/06-intelligent-interchange-decision-gate/06-07-SUMMARY.md
@crates/ratemypcb-cli/src/main.rs
@crates/ratemypcb-cli/tests/decision_report.rs
</context>

<tasks>

<task type="tracer">
  <name>06-08-01: Verify the existing no-go product surface</name>
  <read_first>crates/ratemypcb-cli/src/main.rs, crates/ratemypcb-cli/tests/decision_report.rs, crates/ratemypcb-core/src/fabrication.rs</read_first>
  <files>crates/ratemypcb-cli/src/main.rs, crates/ratemypcb-cli/tests/decision_report.rs</files>
  <action>Inspect before editing. Verify existing doctor JSON returns `unsupportedFormats: ["ODB++", "IPC-2581"]`, CLI help/plain doctor state both are unsupported, Gerber/X2/Job/XNC/native reconciliation remain supported, and capability-gated unavailable analysis remains not checked. Run the existing focused regression. If it passes, make no product change; if it fails to prove FMT-05, stop and report the exact minimum gap without committing.</action>
  <acceptance_criteria>
    - The focused regression passes unchanged.
    - No product source, test, dependency, fixture, corpus, schema, or asset changes.
    - Neither unsupported format can improve approval or gain a format-badge capability path.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; test "$(git rev-parse HEAD)" = 5e0fa62a5865cdea1a7755c6bedcedab3a64ba07; CARGO_NET_OFFLINE=true cargo test -p ratemypcb-cli --test decision_report --locked schematic_doctor_and_snapshot_expose_capabilities_without_client_policy -- --exact; test -z "$(git diff --cached --name-only)"</automated></verify>
  <done>Existing product behavior proves FMT-05 with no code or test diff.</done>
</task>

<task type="auto">
  <name>06-08-02: Record no-go completion and preserve boundaries</name>
  <read_first>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/06-intelligent-interchange-decision-gate/06-VALIDATION.md</read_first>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/06-intelligent-interchange-decision-gate/06-VALIDATION.md, .planning/phases/06-intelligent-interchange-decision-gate/06-08-SUMMARY.md</files>
  <action>Mark FMT-05 complete, FMT-04 not applicable under no-go, and Phase 6 complete only because the existing focused regression proves the unsupported/not-checked product path. Preserve PRIVATE parser `a4216f6...` as quarantined research. Record that any future ODB++ response can inform only a separately authorized reopening. Run GSD, diff, LSP/lens, public-head, staging, product-diff, and Phase-7 guards. Leave all public changes uncommitted.</action>
  <acceptance_criteria>
    - Canonical Markdown records one exact no-go decision with no custom JSON or ceremony.
    - FMT-03 and FMT-05 are Complete; FMT-04 is Not Applicable, not adopted or implemented.
    - Public HEAD remains fixed, staging is empty, and Phase 7 is untouched.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; test "$(git rev-parse HEAD)" = 5e0fa62a5865cdea1a7755c6bedcedab3a64ba07; git diff --check; test -z "$(git diff --cached --name-only)"; test -z "$(git diff --name-only -- crates tests Cargo.toml Cargo.lock)"; test -z "$(git status --short | rg 'phase7|07-decision-grade' || true)"</automated></verify>
  <done>Phase 6 closes on a verification-only no-go path with native KiCad and Gerber/X2+Excellon unchanged.</done>
</task>

</tasks>

<verification>
Run the unchanged focused unsupported-format regression, primary LSP on its current source/test seam, focused lens as applicable, GSD plan/summary/phase consistency checks, `git diff --check`, fixed public HEAD, empty staging, zero product/dependency/corpus diff, fixed private checkpoint, and zero Phase 7 mutation.
</verification>

<output>Record requirement dispositions, exact planning files, unchanged focused test result, zero product-code change, fixed public/private SHAs, and clean/staged/commit state.</output>
